# RCU-COMPAGNON (TRACÉ) — état du projet et repères pour continuer

Lis ce fichier en premier : il résume ce qui a été décidé avec Ethan (chef de chantier réseaux de chaleur pré-isolés) et où on en est.

## Ce que fait l'appli (index.html à la racine du dépôt = build de ce dossier)
- Plan d'ensemble multi-chantiers, deux conduites (aller rouge / retour bleu), fiches soudure (statut, procédé, contrôle, fils de détection, photos), schéma de bouclage, liste, rôles (soudeur, manchonneur, chef, bureau), catalogue des pièces, import DXF, synchronisation Supabase.
- **Moteur de pièces** (`src/pieces.js`) : chaque ligne est un puzzle de pièces catalogue (barres 12 m entières = rigides, barres coupées = libres, coudes/tés/vannes/réductions = rigides). Outil « Modifier le calepinage » : bleu = bouge d'un bloc, orange = encaisse (barre recoupée / tube créé à la soudure), le reste ne bouge pas ; glisser une soudure = la coulisser ; supprimer une soudure = fusion. Soudures faites protégées : ajustée (statuts/photos gardés) ou « à refaire », infos d'une soudure supprimée rattachées à la voisine — toujours demandé, jamais silencieux.
- **Chaque conduite a son propre puzzle** (axe propre à l'entraxe réel = gaine + 15 cm) : mêmes coudes catalogue des deux côtés, les barres coupées encaissent, soudures décalées.
- **Catalogue** (`src/catalogue.json`, onglet Catalogue) : Renalia = catalogue ZPU Międzyrzecz 2024 (décision Ethan : on se fie au ZPU, coudes jambes 1000 mm), LOGSTOR (Product Catalogue 01/2025, lu par OCR/pages : coudes 2500, tés 45° 3500, bouts nus 220 mm), AXIOM (UNO Isolation 1, 2024, complet), INPAL (PU130 2016). Le DN, la gaine et la série doivent devenir modifiables dans l'appli (demande d'Ethan).
- **Import DXF** (`src/dxfimport.js`) : lecture DXF ASCII dans le navigateur (le DWG est converti en DXF avec LibreDWG côté serveur/assistant), profils de dessin : `JBTP/Mensura` (Nantes : pièces dessinées en blocs « Présentation » avec calques internes Tube/Isolation, coudes/tés en blocs, étiquettes « DN50 (60,3/125) », réductions « Réduction DN80 x 50 »), et générique (axes en polylignes → genLine). Aperçu coloré + questions avant création (écarts de dessin : convention → absorbés par les barres coupées / vrais trous → gris). Le dessin d'origine devient le fond de plan (sheet type `vector`).
- **Supabase** (`src/sync.js`) : projet `pghftlepduvfazbiavhq` (Francfort), clé publishable dans le fichier, tables sites / line_state / welds / events + bucket photos, RLS « authenticated ». Connexion par lien e-mail. Hébergement : GitHub Pages https://ethanlb-scr.github.io/RCU-COMPAGNON/

## Processus d'import validé avec Ethan (6 étapes)
1 lecture du dessin (calques, blocs, textes, cotes) → 2 reconstruction du réseau (aller/retour appairés, antennes aux tés) → 3 puzzle par ligne avec le catalogue → 4 contrôles (barre > 12 m, chute < 1 m, angle > 3° à une soudure, DN sans réduction, saut d'axe, pièce non lue, antenne sans té) → 5 questions au chef une par doute avec l'extrait du plan sous les yeux, et pose manuelle « à la grosse » de pièces du catalogue que le moteur remet d'équerre → 6 validation + rapport, ré-import d'une nouvelle version du plan en conservant les soudures faites.

## Cap fixé avec Ethan le 17/08/2026 (à relire avant tout choix)
Ce qu'il veut : **insérer un DWG et que l'appli calepine le réseau pièce par pièce, tube par tube, bon DN, toutes les soudures sélectionnables** ; quand elle n'y arrive pas proprement, **elle demande** en montrant la zone du plan, il glisse des pièces du catalogue « à la grosse » et l'appli les remet d'équerre. Un puzzle **fiable à 100 %** avec les pièces du catalogue.
Trois règles qui en découlent (diagnostic partagé avec lui) :
1. **Jamais inventer.** Chaque pièce a une origine : lue littéralement dans un bloc du plan / déduite sans ambiguïté (entre deux pièces connues, une droite = N × 12 m + 1 chute) / posée par Ethan. Tout le reste = doute → question avec l'extrait du plan, zone grise tant qu'il n'a pas répondu. Aujourd'hui `recognize()` (pieces.js) interprète en silence (piquage, lyre, baïonnette, fusion < 0,6 m) : c'est LA source du « ça n'a aucun sens ».
2. **Le catalogue est la seule source de cotes** (fournisseur / DN / série). Aujourd'hui `pieces.js` a une table `DB` codée en dur (valeurs ZPU) — l'onglet Catalogue n'est PAS branché sur le moteur.
3. **Un profil d'import par type de plan** : LOGSTOR blocs (déterministe), Renalia (axes + repères), JBTP (blocs Présentation), plan projet BE Mensura (axe + nœuds + étiquettes → calepinage à construire), générique.
Honnêteté sur le « 100 % » : atteignable tel quel quand le plan EST une liste de pièces (LOGSTOR) ; sur un plan d'axes, 100 % = l'appli n'invente rien + les réponses d'Ethan aux doutes.

## En cours / à faire (ordre convenu le 17/08)
1. ✅ **Brique 1 (17/08) : l'appli ouvre les vrais plans.** Lecteur DXF en flux (`parseDXFFile`, jamais tout le texte en mémoire ; 470 Mo lus en 4,7 s dans Chromium), reconnaissance du type de plan (`analyze().profile`), choix des calques du réseau avec rôle aller/retour/axe unique, dédoublonnage des calques qui dessinent le même tracé, zone principale (légendes/détails à des km ignorés), lecture générique par graphe (nœuds Mensura « Regards », marche « le plus dans l'axe », antennes aux jonctions), DN **par tronçon** d'après les étiquettes (y compris blocs Mensura écrits lettre par lettre), vannes/réductions/tés par blocs, réduction posée aux changements de DN. Testé de bout en bout (Playwright) sur Bain PRO, Indre, Saint-Lô jaune et bleue.
2. **Moteur ↔ catalogue.json** (fournisseur / DN / série) — supprimer la table `DB` codée en dur de pieces.js.
3. **Interprétations silencieuses → doutes** (piquage, lyre, baïonnette, fusion de fragments, coude hors catalogue, chicane trop courte, angle à un nœud Mensura : 90° ? 2 × 45° ? déviations aux manchons ?). Le plan PRO de Bain est le plan de référence pour ça (voir « Plans reçus »).
4. **Écran des doutes + pose manuelle** de pièces catalogue (glisser « à la grosse », le moteur remet d'équerre : pièces rigides alignées sur l'axe, barres 12 m + chute entre deux pièces).
5. **Profil LOGSTOR** (composants 2000/2500/3500/4200/5252/4900/5031/5033/5600, sous-blocs Joint_Single) — bloqué : le plan de calepinage LOGSTOR de Bain (« 80572 Bain de Bretagne — Plan de Calepinage.dwg ») n'est pas dans `plans/`, Ethan doit l'ajouter. Le fichier « RCU Bain de bretagne PRO V4.dwg » n'est PAS ce plan (voir ci-dessous).
6. **Profil Renalia** : jaune V4 (axes CANALISATION coupés aux repères de soudure, blocs 10090+/4090+/3290+, étiquettes « ALLER DN65 ENV. DN160 ») et zone bleue V18 (calques par tronçon `A_B_DN300`, `SST004_DN40`… avec blocs de pièces `kj160`, `tm125`, `5090+`, axes `PRO_CH_A_Axe` / `PRO_CH_R_Axe` → lecture quasi déterministe possible). Aujourd'hui les deux passent par la lecture générique (résultat fragmenté : 385 lignes sur la jaune).
7. **DWG directement dans l'appli** : `@mlightcad/libredwg-web` (LibreDWG en WebAssembly, 10 Mo, à charger à la demande comme pdf.js) lit les DWG d'Ethan en Node (Bain 34 Mo : 11 s de lecture + 60 s de conversion JS ; Indre : bug « Invalid array length » dans leur convertisseur). Deux voies : (a) l'utiliser tel quel + adaptateur `DwgDatabase → {ents, blocks, layersCount}` ; (b) recompiler LibreDWG master en WASM avec l'écriture DXF activée et brancher `parseDXFFile` dessus (un seul chemin de lecture, LibreDWG à jour). En attendant : ODA File Converter (gratuit, Windows) sur le PC d'Ethan pour DWG → DXF.
8. DN / gaine / série modifiables par tronçon ; option « appliquer aussi à l'autre conduite » ; conversion serveur / Supabase Pro / dépôt privé + domaine (inchangé).

## Maquette « traceur » (17/08/2026, soir) — nouvelle méthode de travail
Ethan a demandé de **cadrer avant de coder** et de travailler en **maquette isolée** sur des points précis, intégrés ensuite dans l'appli. Cadre validé avec lui : fournisseur de référence **AXIOM** ; tracé aux **sommets** (règle) puis retouche pièce par pièce ; **un axe → deux conduites** déduites à l'entraxe réel ; règles : **chute ≥ 1 m, jambe de coude recoupable jusqu'à 0,5 m, 3° max au manchon** ; **antenne tracée d'abord, té créé automatiquement** ; lyres tracées à la main (4 sommets).
- `app/maquette/` (page seule, construite à part : `npm run build:maquette` → `dist-maquette/index.html` → copié à la racine du dépôt en **`maquette.html`**, servi par GitHub Pages) : `index.html` (UI), `main.js` (état, rendu SVG, interactions, panneaux, fond de plan DXF via `parseDXFFile`+`buildDrawing`, sauvegarde localStorage, export/import JSON), `traceur.js` (moteur pur : `buildConduit` sommets → pièces catalogue posées, `offsetPoly` vraie parallèle, `splitBars`, `nomenclature`), `regles.json` (règles modifiables dans l'onglet Règles).
- `app/src/catalog.js` : **adaptateur catalogue → cotes** (`catalogFor(fournisseur, série)` : gaine, Ø acier, longueurs de barre, bouts nus, coude {jambes, jambes longues, R, tout angle ?, angles std}, té {L,B,H}, réduction, vanne, manchon ; `missing` liste ce qui manque au catalogue → valeur par défaut explicite). C'est la brique « moteur ↔ catalogue » ; `pieces.js` (moteur de l'appli) devra l'utiliser à la place de sa table `DB`.
- Règles implémentées dans `buildConduit` : segments droits entre sommets ; sommet d'angle < devMax → pas de coude, la barre est coupée là et la déviation se prend au manchon (signalée) ; sinon coude catalogue (angle arrondi au degré ; « sur mesure à commander » si hors angles std et fournisseur « tout angle », doute sinon) ; entre deux pièces rigides : barres entières + chute ≥ chuteMin (avant-dernière recoupée si reste < chuteMin) ; pas la place d'une chute → jambe longue (1×2 m) recoupée pour tomber juste, sinon chute courte **signalée en rouge** ; chevauchement → jambes recoupées à parts égales jusqu'à jambeMin, au-delà **erreur** (« les pièces catalogue ne rentrent pas ») ; tés/vannes/réductions/bouchons posés au chaînage cliqué (jamais recoupés) ; deux conduites : `offsetPoly` ± entraxe/2 (l'ancien `offsetPoly` d'app.js n'est PAS une vraie parallèle — sommets faux de 30 cm, angles faux de 1° — à remplacer par celui de traceur.js) ; une antenne démarre exactement au **port de branche du té** de sa parente (aller sur aller, retour sur retour, côté choisi par la géométrie).
- Testé (Playwright `test/maq_ui.mjs`, `maq_test.mjs`) : ligne à baïonnette 2 m → coude contre coude 1+1 des deux côtés, barres qui encaissent (6,42 / 6,08 m), soudures décalées ; antenne DN50 sur DN100 avec té automatique ; vanne ajoutée par le modal.
- **Retour d'Ethan sur la v1** (« l'outil de traçage fonctionne vraiment pas mal ») et corrections faites dans la foulée : (a) **tés décalés** : les deux conduites d'une antenne partent à ± e2/2 de son axe, le té de la conduite aller de la parente se pose au droit de la conduite aller de l'antenne, idem retour → les deux tés ne sont plus au même chaînage (c'est ce que montrent les plans JBTP/Renalia) ; (b) **tracé guidé** : aimant d'angle par défaut (15°, réglable 45°/90°/libre, Maj inverse), formulaire de saisie sous le tracé (longueur + angle relatif gauche/droite, ou cap absolu pour le premier tronçon), boutons **Lyre…** (dos, profondeur, côté → 4 sommets exacts) et **Baïonnette…** (décalage exact, l'aval est décalé) pendant le tracé et via ＋ sur une ligne existante ; (c) visuel minimal : vanne = boîtier + volant, réduction = cône. Ethan a joint l'ancienne maquette « moteur de pièces v0 » (outil Modifier · déplacer bleu/orange) : il veut **mélanger les deux** — v2 = retouche des pièces/sommets dans le traceur.
- **v1.2 (retour d'Ethan : « j'aime, c'est parfait ! »)** : (a) épaisseur de trait selon le DN ; (b) **manchette** (règle `manchetteMin` 0,2 m) : entre deux pièces rigides, quand il n'y a pas la place d'une chute ≥ 1 m, on met une manchette (bout court à deux soudures, id `M…`, comptée à part dans la nomenclature) — plus de « jambe longue recoupée » ; en dessous de 0,2 m, jambes de coude recoupées ; (c) **lyre par défaut selon le DN** : dos sur l'axe = 2 jambes + entraxe → dos intérieur = coude contre coude exact, dos extérieur = coude + manchette de 2 × entraxe + coude ; bras = 2 jambes + chute mini (les bras ont la même longueur sur les deux conduites, ce sont les dos qui diffèrent) ; baïonnette par défaut = 2 jambes (coude contre coude) ; (d) **aimants de longueur** pendant le tracé (idée d'Ethan) : depuis le dernier sommet, repères à 2 jambes (« coude contre coude »), 2 jambes + chute mini, 2 jambes + n barres entières ; en début de ligne : jambe (+ branche du té pour une antenne) puis n barres + jambe ; on accroche à ±0,3 m (ou 7 px), on passe au-dessus pour une longueur libre ; le repère actif est étiqueté en vert ; (e) ＋ : positions aimantées pour té/vanne/réduction (collé après la pièce précédente = fin de jambe, collé avant la suivante, sur la soudure la plus proche). Testé (`test/maq_lyre.mjs`) : lyre DN100 → intérieur coude contre coude 2 m, extérieur manchette 0,70 m ; aimant « 2 m coude contre coude » accroche à 1,9 m.
- **v1.3 — règles d'angle d'Ethan** : (a) **fausse coupe** : tout changement d'angle < `fausseCoupeMax` (15°) se fait sans coude, à une soudure où les tubes sont coupés en biais (≤ `devMax` 3° : simple déviation au manchon, sans coupe en biais) — la barre est coupée au sommet, la soudure porte l'angle (`fc`, « FC x° » au dessin et dans la liste) ; (b) **coudes au pas de 15°** (`coudeAnglePas`) : angle tracé → coude catalogue le plus proche (15/30/45/60/75/90), le reste (≤ 7,5°) en fausse coupe à la soudure aval si un tube suit (le tube démarre au bout de la jambe : les tubes en aval sont décalés de quelques cm, absorbés au tube suivant), sinon amont ; noté dans les infos de la pièce et les notes ; (c) **manchons dessinés** aux soudures (longueur catalogue, gris) : deux soudures d'une manchette courte tombent dans le même manchon — répond à la remarque « trous » d'Ethan (les bouts nus de 15 cm sans isolant, en gris clair avec l'acier fin dessus, ressemblaient à des trous : ce sont les bouts nus, le manchon les couvre). Question ouverte à Ethan : sa règle par défaut entre deux coudes proches — manchette (dès 0,20 m) ou jambes recoupées ?
- **v1.4** : (a) **vannes en quinconce** (option dans ＋ : la vanne retour décalée de d m le long de la ligne, défaut L vanne + 0,5 m — attribut `staggerR` du spécial, appliqué à la conduite R) ; (b) les « trous » d'Ethan (image reçue) = manchettes très courtes (≤ 2 bouts nus) dessinées comme bouts nus (clair + trait acier fin) → maintenant dessinées entièrement en acier nu (trait sombre) sous le manchon, lisibles.
- **v1.5 — pose en drapeau (précision d'Ethan : « visuel pour que les opérateurs choisissent la bonne soudure, pas d'autre contrainte, entraxe classique ; une canalisation passe au-dessus de l'autre avec une surlongueur et un jeu de coudes »)** : ＋ → « Drapeau » sur une ligne (PK début, longueur, conduite dessus, coudes 2 × 15°/30°/45°) → `line.drapeaux[]`. La conduite du dessus se rabat en plan sur l'axe de l'autre (diagonale de longueur D·cos A, `insertDiag`) en montant d'un entraxe : déplacement δ = e√2 pris par **2 coudes 3D** (spéciaux `bend3d`, D = δ/sin A entre coudes, ≥ 2 × jambeMin ; coude contre coude à jambes recoupées ou manchette entre selon D) ; les sommets de la diagonale sont « couverts » par les coudes 3D (pas de fausse coupe) ; à la fin, l'inverse. La conduite du dessous est dessinée en pointillé sur le tronçon ; le panneau de la ligne montre le drapeau avec une **vue en élévation** (hauteur pas à l'échelle) et permet de le supprimer. Coudes 3D étiquetés « ↕ » et « en altimétrie (jeu de coudes, monte/descend) », comptés dans la nomenclature, soudures numérotées comme les autres. DN100 : 2 × 15°, D = 1,91 m, jambes 0,96 m recoupées.
- **v1.6 — retours d'Ethan du 17/08 (soir)** : (a) **sortie de té = soudure** : une antenne démarre au bout de la branche du té de la parente (distance catalogue B) par une vraie soudure (pièce fantôme `teeBranch` « T1↳ », soudure `teeOut` à double trait, dans la liste et la numérotation) — plus de faux « départ de ligne » ; (b) **inverser aller/retour** ligne par ligne (`line.inv`, bouton ⇄ dans la fiche : aller à gauche du tracé par défaut ↔ à droite) — offsets, tés, drapeaux suivent (`offOf`, `sgnOf`) ; (c) **tés à saut en quinconce** : la conduite parente du côté de l'antenne reçoit un té droit ; l'autre (« lointaine », déterminée géométriquement : la plus loin d'un point à 1 m sur l'axe de l'antenne) reçoit un **té à saut** (`te_a_saut_45_TW` chez AXIOM — `sautType()` cherche /saut/ dans `cat.teeTypes()`), **décalé de `teQuinconce` (2 m, règle) le long de la parente** dans le sens qui éloigne sa branche de la conduite d'antenne voisine ; la conduite d'antenne correspondante repart du port par une jambe de coude puis une **baïonnette 2 × 90°** (coude contre coude si d = 2 jambes) pour revenir à l'entraxe (`cp=[port,P1,P2,…]`) ; branche du té à saut dessinée par-dessus la conduite voisine (halo clair, « saut ↑H ») ; réglable par antenne (fiche ligne : quinconce d, 0 = côte à côte) ; ＋ → Té : même option (quinconce auto avec té à saut, décalage ± aval/amont, ou côte à côte) — `sp.stagger/staggerDir/teeTypeFar` ; (d) **traits selon DN** : épaisseur = gaine × max(1, 24/k) → à l'échelle dès 24 px/m, proportions gardées en dessous (DN50 plus fin que DN100) ; (e) **extrémités** : `line.endType` (kit fin de ligne [défaut] / fin de ligne provisoire / by-pass / sous-station / libre) et `line.startType` pour les lignes principales (raccordement chaufferie-sous-station [défaut] / reprise sur fin provisoire / kit / libre) → nœuds `endcap` (F, 1 soudure), `endpoint` (X, raccordement, 1 soudure, hors nomenclature), `bypassEnd` (U : coude 90° recoupé + arc R = e/2 vers l'autre conduite, soudure aller↔retour à l'apex portée par l'aller → 3 soudures ; **doute** noté : à l'entraxe standard les jambes pré-isolées ne rentrent pas — coudes acier nus isolés sur place ou entraxe élargi, **à cadrer avec Ethan**) ; le « Bouchon de fin » de ＋ est retiré (c'est un réglage de la ligne) ; (f) **lyre : profondeur mini par défaut** (bras = 2 jambes → coude contre coude, plus de chute dans les bras ; dos intérieur coude contre coude, dos extérieur + manchette 2e — inévitable) ; (g) **fausses coupes en évidence** : rond de soudure orange plein + « FC x° » en gras sous le numéro dès k ≥ 3, compteur orange dans la fiche ligne et le pied de page, badge dans l'onglet Soudures. Test `app/test/maq_v16.mjs` (tés/quinconce, inversion, extrémités, lyre, FC, épaisseurs, té manuel). Question à Ethan : valeur du décalage des tés à saut sur le terrain (2 m ?) et réalisation d'un by-pass de fin de ligne.
- **v1.7 — retours d'Ethan sur la v1.6 (images)** : (a) **tés à saut : côte à côte, pas de coude en sortie** (« c'est nul » la baïonnette) → quinconce/baïonnette **retirés** (règle `teQuinconce` supprimée) ; chaque té reste au droit de sa conduite d'antenne (décalés de l'entraxe de l'antenne), té droit côté antenne, té à saut sur l'autre conduite (branche dessinée par-dessus), l'antenne repart droit du port ; (b) **inversion A/R** : Ethan précise que c'est l'antenne qui s'inverse, pas le feeder existant → ⇄ sur une antenne échange ses deux conduites, les deux tés s'échangent sur la parente (déplacés de e2), la parente ne bouge pas (vérifié par test) — le bouton reste sur toutes les lignes ; (c) **tés de purge / vidange** : ＋ → « Té de purge / vidange » (branche verticale : purge ▲ vers le haut, vidange ▼ vers le bas ; posé sur les deux conduites au même PK ; DN de branche `purgeDN` 25 / `vidangeDN` 40 par défaut, réglable) → spécial `{type:'tee', vert:'up'|'down', dnb, photo}` ; pièce `tee` avec `vert`, `center` (symbole rond + triangle, pas de branche en plan), ref « té de purge DN100/25 — TP - 100/25 (té droit, branche verticale) » (type catalogue dédié s'il existe : Renalia TD/TO) ; **en bout de ligne** : fiche ligne → Extrémités → « purge / vidange en bout » = spécial `atEnd` recalé à chaque reconstruction juste avant la pièce de bout de chaque conduite (longueur réelle, DN en vigueur à la fin) ; **prise de vue** : fiche de la pièce → 📷 (input file `capture=environment`, vignette JPEG 640 px max en dataURL dans le spécial → sauvegarde navigateur / export JSON ; 📷 sur le tracé) ; « Té d'attente » (ancien té sans tracé) gardé, côte à côte ; (d) **réduction : le DN baisse après** — `buildConduit` : `dnAt(m)` (DN en vigueur : dn puis dn2 après chaque réduction), tubes/coudes (jambes catalogue du DN local)/spéciaux/pièces de bout prennent le DN local (gaine, refs `R-65/140`, `K – 65/90`), soudures au DN aval de la réduction, manchons et ronds de soudure au DN de la soudure ; l'entraxe reste celui du DN de la ligne (écart de quelques cm ignoré) ; (e) **fiche d'une pièce de bout** (kit, raccordement, by-pass) : sélecteur du type de départ/fin ; réponse à Ethan : le **statut** (à faire / soudé / contrôlé) n'existe pas dans la maquette, il vit dans l'appli TRACÉ ; (f) `catalog.js tee()` : quand le DN de branche exact n'est pas dans la table (le catalogue AXIOM transcrit ne liste qu'une branche par DN), la référence est réécrite avec le vrai DN de branche (`TP - 100/25`) et `exact:false`. Test `app/test/maq_v17.mjs`.
- **Passerelle traceur → TRACÉ (17/08 soir, « feu » d'Ethan)** — décision : le traceur reste **une page à part** (`traceur.html`, bureau : calque DXF + tracé à la main ; `maquette.html` = redirection), l'appli reste le terrain. Ethan : « les réseaux qu'on importe = image de fond en calque, je trace par-dessus à la main » ; l'import DXF de l'appli n'est plus proposé (code gardé). `app/maquette/bridge.js` : `siteFromTraceur()` → chantier au format `sites` de l'appli : `lines[]` avec `traceur:true`, `cond.A/R = {els (kind pipe/bend/tee/valve/reducer/endcap/endpoint/bypass/teeout, casing en mm, axis [[x,y]…], m0/m1, refs, vert/photo…), welds (idx, weldId, m, fc, dev, dn, teeOut, bypass)}`, `axis`, `e`, extrémités ; `drawing` = fond de plan du traceur (`sheetType:'vector'`, sinon `'plain'`), `traceur:{lines (source), rules, bgName}` pour ré-ouvrir ; **numéros de soudure stables** : appariement avec la version précédente (même ligne/conduite, PK ± 0,35 m) → statuts et photos survivent aux retouches ; soudures faites non retrouvées → confirmation + `report.lost`. Traceur : bouton **💾 Enregistrer dans TRACÉ** (nom, mise à jour ou nouveau) → remise locale `localStorage trace:handoff:<id>` (sans le dessin si trop lourd) + `sync.saveSite` si connecté (session Supabase partagée, même origine) → « Ouvrir dans TRACÉ » (`index.html?site=<id>`) ; `traceur.html?site=<id>` recharge un chantier (handoff local, sinon serveur + `loadWelds` pour les statuts), `?site=new` repart à vide ; badge « chantier : … ». Appli : `setupTraceurLine()` (deux chaînes de pièces déjà posées, `ownAxis`, `axisHalf = e/2`, pas de moteur → l'outil bleu/orange renvoie vers le traceur), `loadHandoffs()` au démarrage + `pushHandoffs()` à la connexion, `?site=` sélectionne, boutons **✎ Traceur : créer / modifier ce réseau**, **＋ Nouveau réseau**, **🗑 Supprimer ce chantier** (`sync.deleteSite` : welds, line_state, events, sites ; sinon SQL affiché ; chantiers intégrés masqués via `trace:hiddenSites`), « Rapport du tracé » (notes du traceur, soudures perdues), liste des soudures avec DN local et FC. Feuille `plain` (fond uni, sans carroyage ni texte en unités monde). Test `app/test/e2e_bridge.mjs` (serveur statique sur /tmp/site : tracé → enregistrer → appli (liste, fiche) → retour traceur (`?site=`) → retouche → mise à jour (S-0001 conservé, vanne = numéros neufs) → suppression). Reste : aimant sur le calque DXF pendant le tracé, retouche paramétrique (v2), export DWG→calque côté appli (le traceur charge déjà un DXF).
- **Retours d'Ethan sur la passerelle (image 1 : chantier dézoomé « pas beau » ; « l'import DXF fonctionne pas ; l'idée c'est juste une image en arrière-plan à faible opacité, afficher/masquer, tracer par-dessus »)** : (a) appli, `renderPlan` : en dessous de 6 px/m (`far`), un trait fin par conduite (2,2 px, écart 2,4 px, aller rouge / retour bleu teinté par le statut aval) au lieu de la gaine noire à 5 px mini + écart 4,5 px qui faisait des « saucisses » ; (b) **fond image dans le traceur** : 🖼 Image (PNG/JPG/capture, réduite à 2 200 px, JPEG) → posée à l'écran → mode **📏 Échelle** (deux clics sur une distance connue + saisie en m ; le premier point reste fixe), **✥ Déplacer** (glisser), **opacité** (curseur, 50 % par défaut), **afficher/masquer**, ✕ ; `state.bg = {drawing?, image:{src,pw,ph,x,y,w,h}, bbox, opacity, show, name}`, image persistée dans `localStorage trace:bg:v1` (le DXF non : trop lourd, à recharger après F5) ; le DXF garde sa place (📐) avec la même opacité / masquage ; (c) le fond image part dans le chantier (`site.image {src,x,y,w,h,opacity}`, `sheetType:'plain'` sans DXF) et l'appli l'affiche sous le réseau (`sh.image` → `<image>` dans le fond, opacité du traceur) ; rechargé dans le traceur avec `?site=`. Test `app/test/e2e_bridge.mjs` + scénario image (`_img`, non gardé) : échelle 600 px = 50 m → 0,0833 m/px ✓, opacité, masquage, handoff avec image, appli. À demander à Ethan : le message / le fichier de son import DXF raté (DWG non converti ? DXF binaire ? taille ?).
- **Fond DXF pour de bon (Ethan : « je reçois les plans en DWG, il faut importer les DXF, le seul import est d'en faire un arrière-plan enlevable en un clic »)** : (a) traceur 📐 DXF : **barre de progression** pendant la lecture (`parseDXFFile(f, onProgress)`), refus explicite d'un `.dwg` (message : exporter en DXF ASCII 2013), erreur lisible si binaire ; puis **modale des calques** (`openDxfModal` : calques triés, candidats « axe RCU » précochés d'après `analyze()`, nombre de traits ; contexte autour du réseau 60/200 m / tout / rien ; détail normal 120 000 points / léger 40 000 / fin 300 000 ; « même repère que le fond précédent ») ; **zone principale** du réseau par cellules de 500 m (la plus chargée + voisines, 8-connexité) pour ignorer légendes et copies à des km ; **repère** = origine arrondie à 100 m (`bg.origin {x0,y1}`, y inversé) mémorisée dans le fond, le chantier (`traceur.bgOrigin/bgLayers`) et réutilisée à la réimportation d'une nouvelle version du plan → le réseau déjà tracé reste calé ; `buildDrawing` reçoit l'emprise **dans le repère transformé** (bug corrigé : 0 trait) ; (b) fond gardé dans **IndexedDB** (`app/src/kv.js`, clé `bg:dxf`) → revient après F5 ; la remise traceur→appli passe aussi par IndexedDB (`trace:handoff:<id>`, localStorage en secours) : plus de limite de 5 Mo ; (c) **calque zoomé** : au-delà de 6 px/m, seuls les traits visibles (± un demi-écran) sont mis dans le SVG (traceur `renderBgIfNeeded` sur `applyView`, appli dans `renderPlan` avec clé de vue) — piste sérieuse pour « au-delà de 17 px le tracé disparaît » (Chrome ne peint plus un calque de plusieurs km à fort zoom) ; (d) `fitAll` du traceur cadre sur les lignes (le fond seulement s'il n'y a rien de tracé) → « quand je veux modifier je ne retrouve pas mon réseau » ; (e) appli : branche des tés dessinée (jusqu'à la sortie où repart l'antenne, té à saut par-dessus). Mesures headless : `bain.dxf` 279 Mo lu en 5–9 s (PC d'Ethan : compter 30–60 s), 197 calques, 2 calques RCU précochés, 25 500 traits (461 réseau) autour de la zone principale 1,1 × 1,4 km ; F5 → fond restauré ; chantier avec fond → appli OK ; zoom 30 px/m : 2 tuyaux du plan lisibles. Tests `app/test/e2e_dxf.mjs` (fichier en argument), `e2e_bridge.mjs` (IndexedDB).
- **Fond DXF automatique (Ethan : « choisir les calques c'est relou, le logiciel doit comprendre de lui-même ; échec St-Lô ; l'import Indre pareil ») + « le réseau disparaît après Enregistrer » + « boutons en double »** : (a) `dxfimport.buildBackground(dxf,{netLayers,cap,margin,keepOrigin,zoneAll,units})` : **tout le dessin** (entités + blocs sur 3 niveaux, sans filtrer les INSERT par leur point d'insertion — c'était le trou d'Indre : le fond entier est un bloc), calques poubelle exclus (cartouche/légende/hachures/carroyage), **zone principale = amas de cellules de 250 m le plus dense en nombre de traits** (+ voisines ≥ 2 %) — un cadre de 4 km ne pèse rien, les légendes à des km sont ignorées —, unités `$INSUNITS` mm/cm converties (contrôle 20 m–50 km), repère à l'origine arrondie (ou imposée), réseau reconnu (candidats `analyze`) en foncé sinon tout en clair, tri **réseau d'abord puis du plus long au plus court** sous la limite de points (200 000), Douglas-Peucker 0,1 m, traits < 0,3 m ignorés ; parser : `createDXFParser({blockCap})`, le traceur passe 400 000 (l'appli garde 20 000) ; (b) traceur : plus de modale à l'import → fond automatique direct, ⚙ = options (calques réseau, étendue, détail) sur le DXF gardé en mémoire ; mesures : Bain 279 Mo → 56 700 traits, Indre 15 Mo → 26 100 (le réseau JBTP à double trait), Saint-Lô jaune 259 Mo → 40 000 (zone 2,1 × 1,6 km) ; (c) remise traceur→appli : IndexedDB **et** double léger dans localStorage, liens entre les deux pages avec `&v=<horodatage>` (cache GitHub Pages : index.html restait l'ancienne version → « le réseau disparaît ») ; masquage des chantiers supprimés **horodaté** (`trace:hiddenAt`) : un chantier ré-enregistré par le traceur après suppression réapparaît (avant : le traceur gardait l'id → invisible) ; (d) appli : boutons dédoublonnés (Rapport / Modifier le calepinage seulement s'il y a un réseau non traceur ; « ＋ Nouveau réseau » seulement sur un chantier traceur ; Supprimer seulement s'il y a un chantier), chantier vide en feuille `plain`. Test `e2e_bridge.mjs` étendu (ré-enregistrement après suppression). Reste : St-Lô bleue (469 Mo) non testé ici ; si le zoom « disparaît » persiste chez Ethan après Ctrl+F5 des deux pages, demander capture + navigateur.
- **Bug reproduit : « j'enregistre et ça disparaît » (Ethan, appli sans chantier)** — quand tous les chantiers sont supprimés/masqués, l'appli démarre sur le chantier vide `__vide` et `renderBouclage()` plantait (`mains[0].id` sur une liste vide) → tout le démarrage s'arrêtait après le rendu : pas de boîte cloud (« je n'ai pas l'air connecté »), pas de lecture des chantiers remis par le traceur. Corrigé (message « aucune ligne » dans le schéma de bouclage). En plus : `sync` n'attend jamais plus de 4 s `getSession()` (verrou d'auth partagé entre les onglets traceur/appli, réseau coupé, projet Supabase en pause) et le traceur borne l'envoi serveur à 8 s — l'appli continue en local ; boîte cloud affichée dès le démarrage ; retour sur l'onglet TRACÉ (`visibilitychange`) → relecture des chantiers remis (toast « Reçu du traceur ») ; chantier vide sans `source:'traceur'` (libellés d'outils). Test `_vide` (appli vide → traceur → enregistrer → appli affiche, boîte cloud présente).
- **Vocabulaire d'Ethan : « calque » = l'arrière-plan grisé sur lequel il repasse (pas les calques du DWG). Demande : « le DWG importé en l'état, transformé en image de fond, avec l'opacité »** → `buildBackground` v2 : **tout le dessin** (plus de découpe à la zone : la zone principale ne sert qu'au repère et à la priorité sous la limite), **textes** compris (TEXT/MTEXT nettoyés, hauteur/rotation, blocs inclus ; `{layer,text,x,y,h,rot,net}` dans `drawing`), limite 800 000 points (réseau > zone ± 200 m > le reste, du plus long au plus court), Douglas-Peucker 0,1 m ; rendu : traits gris (réseau reconnu plus foncé), textes dès 4 px/m et ≥ 4 px de haut, **culling à l'écran dès 2 px/m** (traceur `renderBg`, appli `renderPlan`) ; le fond entier reste dans IndexedDB du traceur (Saint-Lô jaune : 40 000 traits + 24 500 textes, restauré en 0,3 s ; Bain : 91 000 + 12 300), le **chantier n'embarque qu'un fond réduit** autour des lignes (`reduceDrawing` ± 150 m, 200 000 points → 0,2–0,4 Mo) pour l'appli et le serveur ; ⚙ options renommées « priorité / détail ». Pas de couleurs de calques (parser sans code 62) ni hachures ; c'est un calque gris volontairement. Test `e2e_dxf_full.mjs`.
- **v2 retouche (Ethan : « rendu enregistré nickel » ; purges absentes du rendu appli ; points cliquables au premier plan + hit box ; « remettre ce qu'on avait fait sur l'autre maquette : déplacer des pièces en choisissant où la modif travaille et ce qui reste figé ensemble ; déplacer n'importe quelle vanne ou soudure, un outil qui coulisse »)** — choix : la retouche est **paramétrique dans le traceur** (le réseau se reconstruit selon les règles, les numéros de soudure survivent via l'appariement) : (a) **glisser un sommet** (poignées `.vtx` de la ligne sélectionnée ou de la ligne de la pièce sélectionnée) ; départ d'antenne : glisse le long de la parente (`parent.m`, `parent.side` recalculés) ; les antennes suivent leur parente (`pts[0]` recalé à `parent.m` à chaque `rebuild`) ; (b) **Maj+clic** sur plusieurs sommets → `state.selV`, glisser l'un = le bloc bouge d'un seul tenant (une lyre entière) — c'est le « figé ensemble » ; « là où la modif travaille » = les barres entre sommets se réorganisent (barres entières + chute) — et l'utilisateur choisit où tombe la chute en coulissant une soudure ; (c) **glisser une pièce spéciale** (vanne, réduction, purge/vidange, té d'attente ; pas les tés auto ni les bouts) le long de sa ligne (`sp.m`) via une zone de prise transparente posée au-dessus des symboles ; (d) **glisser une soudure entre deux tubes** → **coupe forcée** `line.cuts[{id,m}]` (chaînage sur l'axe, projeté sur chaque conduite) → `buildConduit({cuts})` : nœud rigide `cut` (L 0) → barres entières + chute de chaque côté ; une coupe qui tombe dans une pièce rigide est retirée ; listées dans la fiche ligne (✕) ; seules les soudures tube–tube coulissent (curseur ↔) ; (e) **double-clic sur l'axe** : sommet inséré ; **Suppr** : sommet sélectionné supprimé (2 mini, pas le départ d'antenne) ; **Ctrl+Z / ↶** : historique (`snapshot()` avant chaque retouche, 40 niveaux) ; `rebuild({light:true})` pendant le glisser (pas de panneau/sauvegarde). Appli : symbole purge/vidange dessiné (`e.vert`), **soudures en second passage** (toujours au premier plan, `netJ`), zone de prise transparente 16 px mini sur chaque pièce. Tests `maq_edit.mjs`. L'outil bleu/orange (`pieces.js`) reste pour les anciennes lignes importées.
- **Retours v2 (« premier plan pas mal mais on ne sait pas quelle soudure est visée » ; « le double-clic pour ajouter un sommet ne semble pas fonctionner »)** : appli — chaque pastille de soudure est reliée à sa soudure par un **trait** et porte un **anneau à la couleur de la conduite** (rouge aller / bleu retour), pastille un peu plus loin du tube ; recherche « N° de soudure » accepte les numéros à 4 chiffres (S-0024, avant : S-024 seulement). Traceur — double-clic **près de n'importe quelle ligne** (plus besoin qu'elle soit sélectionnée, tolérance max(3 m, 14 px)) insère un sommet ; en secours, ＋ → « Sommet » au PK cliqué.
- **Règle Ethan (après ses coudes déplacés « n'importe comment ») : « au lieu de petits trous il faut des manchettes nues avec fausses coupes qui passent dans un seul manchon — moins de 20 cm = 2 soudures dans un manchon »** → `buildConduit` : entre deux pièces rigides, 0,03 m ≤ écart < manchetteMin → **manchette nue** (`nue:true`, id M…, ref « manchette nue x m (acier nu, un seul manchon) »), deux soudures marquées `sleeve` = id de la manchette (même manchon), les résidus d'angle des coudes voisins (coude au pas de 15°) tombent en fausse coupe à ces soudures ; < 3 cm → jambes recoupées sinon note « écart absorbé à la soudure » ; **plus jamais d'erreur/trou** dans ce cas. Nomenclature : « manchettes nues (un seul manchon) » à part. Bridge : `nue`, `sleeve` → `sleeveWith` (numéro de l'autre soudure du même manchon) ; appli : manchette nue dessinée en acier (gris) même au zoom détail, fiche soudure : chip « même manchon que S-… ». Test `maq_nue.mjs` (62°/47° : C1 60° + FC 2°, M1 0,13 m nue, S-0005/S-0006 même manchon ; retour : deux manchettes nues 0,07 / 0,08).
- **18/08 — retours d'Ethan : (1) « l'identification des soudures est parfaite ; il faudrait juste cocher/décocher ce qu'on affiche (noms des pièces, longueurs…) pour épurer une zone » ; (2) après une première livraison qui refaisait tout le rendu au zoom (gaine couleur, manchons clairs, pastilles étagées) : « les tubes sont devenus rouge et bleu, je voulais rester en noir avec des petits indicateurs comme avant ; l'acier quand c'est pas soudé a disparu ; il y avait juste ça à ajouter, tu as retouché plein d'autres trucs »** → **rendu du plan remis à l'identique** de la version qu'il validait (gaine noire 1,7 ×, bouts d'acier + tranches jaunes, colliers rouge/bleu, pastilles reliées, étiquettes) — vérifié : mêmes comptes SVG (34 textes / 300 chemins à 15 px/m…) ; **seul ajout** : bouton 👁 dans les commandes de zoom → panneau « Affichage » (mémorisé sur l'appareil, `trace:show`) : noms des pièces, longueurs et angles, pastilles de soudure, n° de soudure, manchons posés, fils, fond de plan, textes du fond, annotations ; « Tout » / « Épuré » (épuré = réseau + pastilles). Même panneau dans le traceur (`trace:traceur:show` : noms, cotes, n°, FC/déviations, manchons et bouts nus, noms de lignes, grille, fond, textes du fond). Petit plus gardé : l'appli **rouvre le dernier chantier** consulté (`trace:lastSite`) au lieu de la démo Bain. Leçon : quand Ethan dit « juste ça », c'est juste ça — pas de refonte à côté.
- **18/08 — DXF « pas lisible » (Indre : textes géants « MATERNELLE », « Phase 6 », « DN50 (60,3/125) » de 10 m ; question « on abandonne le DXF ? je peux pas juste avoir l'image du DWG en fond ? »)** : cause trouvée dans le parseur — les MTEXT AutoCAD 2018+ portent une section `101 Embedded Object` dont les codes 40/41/42 (largeur de colonne…) écrasaient la hauteur du texte (0,9 m devenait 9,55 m) → `createDXFParser` ignore tout après un code 101 jusqu'à l'entité suivante ; en plus, un texte de plus de 60 px à l'écran n'est plus dessiné (titres/phasage : ils reviennent en dézoomant). L'ortho-photo du DWG est une image externe (entité IMAGE) : elle n'est pas dans le DXF → pour l'avoir, capture/PNG du DWG puis 🖼 Image + 📏 Échelle. Reste à faire : **couleurs du DXF** (calques/entités, ACI → RGB) pour que le fond ressemble au DWG (tube rouge/bleu, isolation gris) au lieu d'une bande grise ; hachures (zones de phase) non lues.
- **18/08 — lecteur DXF « universel » (go d'Ethan : « dès que je pose un DXF, il faut que ça reproduise exactement le DWG d'origine, sans adaptation chantier par chantier »)** — étape 1 livrée. Principe : le lecteur applique les **règles du format** (celles d'AutoCAD), validé sur un **jeu de test généré** (`tools/gen_dxf_tests.py` avec ezdxf → `test/dxf/fidelite.dxf` + rendu de référence `fidelite_ref.png` par ezdxf ; `test/dxf_fidelite.mjs` rend le même fichier dans le traceur → `fidelite_traceur.png` à comparer) et sur les plans réels (Indre, Bain). Fait dans `dxfimport.js` : (a) parseur : table des **calques** (couleur, éteint = couleur négative, gelé = flag 1, type de trait, épaisseur, transparence xdata), table des **types de trait** (49), **IMAGEDEF** (section OBJECTS : chemin des images externes), codes 5/6/7/62/370/420/440 sur toutes les entités, TEXT 72/73 + point 11/21 (alignements), MTEXT 71 (accroche) / 41 (largeur) / 44 / vecteur 11-21 (rotation), ATTRIB complets (position, hauteur, alignement, invisible), **HATCH** (automate dédié `hatchCode` : boucles polylignes avec bulges, boucles d'arêtes ligne/arc/ellipse/spline, plein/motif, chaque boucle finie par 97 + 330), **SPLINE** (points de contrôle + nœuds + poids, ou points d'ajustement), **ELLIPSE**, **LEADER**, **SOLID/TRACE/3DFACE**, **IMAGE/WIPEOUT** (cadre), **DIMENSION** = son bloc `*D…`, `999 LibreDWG` (écrivain) ; (b) `buildBackground` v3 : couleur résolue comme AutoCAD (vraie couleur > ACI de l'objet > par bloc = insertion > par calque ; objet du calque 0 dans un bloc = calque de l'insertion ; **palette ACI 256** exportée `ACI/aciHex` ; ACI 7 = noir sur fond clair), **règle LibreDWG** : ses 420 recopient le champ brut du DWG → on ne garde une vraie couleur que si l'octet haut vaut 0xC2 (sinon on suit l'index 62 — vérifié sur Indre : phases orange/bleu/vert/magenta comme dans TrueView, alors que le 420 disait bleu nuit), calque **gelé** = tout le bloc disparaît, **éteint** = seuls ses objets du calque 0 (règle AutoCAD, même comportement qu'ezdxf), épaisseurs (`w` en 1/100 mm → 1–4 px), types de trait (motif de la table ou nom → `stroke-dasharray` en px), transparence 440 / calque, hachures pleines à 0,55 max et motifs à 0,28 (fond de plan : nos conduites restent lisibles), textes : alignements TEXT/MTEXT (`a`, `va`), plusieurs lignes (`n`, `\P`), retour à la ligne à la largeur du cadre, texte aligné entre deux points (`tl` → `textLength`), codes de mise en forme MTEXT nettoyés (\A \C \f \H \Q \T \W \p \L \O \K \S {} %%c %%d %%p %%nnn), taille de police = 1,38 × hauteur (hauteur AutoCAD = capitales), plus de calques « JUNK » exclus (tout est dessiné), blocs insérés jusqu'à 30 000 fois, **rapport de fidélité** (`report` : traits, hachures, textes, couleurs, calques éteints, images externes, objets propriétaires, types non dessinés, blocs tronqués) ; (c) rendu partagé `drawingSVG(drawing,{k,box,bb,texts,colors,op,fillOp})` + `textSVG` + `drawingBBoxes` (hachures dessous, traits groupés par couleur/épaisseur/type, textes dessus ; textes < 4 px ou > 60 px non dessinés) utilisé par le traceur (`renderBg`, re-rendu au zoom même sur petit dessin) et l'appli (`renderPlan`, vue entière en cache + vue zoomée) ; case 👁 « Fond en couleurs » (sinon l'ancien gris) ; bouton **ⓘ** du fond = rapport de fidélité (modale). Reste (étape 2) : motifs de hachure dessinés (pour l'instant aplat clair), MLEADER/MLINE/TABLE, polices .shx (Arial partout), images externes déposées à côté du DXF (calage automatique), lecture des DXF binaires.
- **18/08 — Ethan : « ça marche incroyablement bien » (Saint-Lô zone bleue décalquée entière, 1 667 soudures) + remarques** : (a) **listes de chantiers différentes téléphone / PC** : cause = un chantier supprimé depuis un appareil (serveur + masqué localement) était **ressuscité** par l'autre appareil dont la copie locale (IndexedDB) était renvoyée au serveur à chaque connexion → `pushHandoffs(remote)` : marque `sent` après envoi ; n'envoie que le nouveau ou le plus récent (`traceur.savedAt`) ; un chantier déjà envoyé absent du serveur = supprimé ailleurs → retiré localement (`removeSiteOption`) ; ordre à la connexion : `loadSites` puis `pushHandoffs(remote)` ; (b) **fils** visibles dès 15 px/m (au lieu de 30) ; (c) **gros DN : gaines collées** dans l'appli (gaine dessinée 1,7 × > entraxe) → axes écartés d'au moins « gaine dessinée + 2 px » (hors mode loin), puis (« un peu mieux mais pas parfait ») **gaine dessinée à 1,15 × au zoom détail (≥ 12 px/m)** au lieu de 1,7 × : l'entraxe réel se voit (DN100 : 7 px de jour à 47 px/m) ; (d) traceur : **entraxe selon le DN** — règle `entraxeSupParDN` {"100":0,15, "200":0,20, "300":0,25, "1000":0,30} (écart entre gaines = plus d'espace d'emmanchement sur les gros DN ; valeurs à confirmer par Ethan, modifiables dans l'onglet Règles), utilisée partout où `entraxeSup` l'était ; (e) **réseau hors écran** (fausse manip) : bouton central « ↩ Réseau hors écran — revenir » dans le traceur (`offscreenCheck` à chaque `applyView`) et dans l'appli (`renderPlan`, emprise de la feuille vs fenêtre) → recadrage. Test `test/e2e_misc.mjs`. Sujets suivants listés par Ethan : interface, comptes/droits d'accès, questionnaire à la prise de vue, récap pièces/soudures par DN.
- **18/08 — onglet « Récap »** (`renderRecap`, `recapData`, `statusCounts`) : en-tête (lignes, ml de tracé aller, soudures, % soudées ou plus, % manchonnées, barre d'avancement par statut), **soudures par DN** et **par ligne** (total, à souder, soudées, contrôlées, manchonnées, à reprendre, fausses coupes, %), **nomenclature du calepinage** (barres entières / coupées / manchettes / manchettes nues par DN avec ml, coudes par angle, tés droit / saut / purge / vidange avec DNb, réductions, vannes, kits, by-pass ; référence catalogue), **manchons à poser** par DN (deux soudures d'une manchette nue = un manchon), exports **CSV** soudures (n°, ligne, conduite, DN, PK, statut, FC, sortie de té, même manchon, fils, dernier événement, par, date, photos, note) et pièces — séparateur « ; », BOM UTF-8 (Excel FR). Test `test/e2e_recap.mjs`.
- **18/08 — onglet « DH »** (remplace « Schéma de bouclage », Ethan : « quand on se met à un point, en fonction de ce qui est raccordé, ça doit dire les mesures qu'on doit trouver, avec un récap de boucle ; le schéma en ligne est illisible ») : (a) **paramètres accessibles** (résistance d'un fil Ω/km, défaut 12,5 = cuivre 1,5 mm² — Ethan n'a pas la valeur, à confirmer avec le fournisseur ; isolement mini MΩ, défaut 200) mémorisés sur l'appareil (`trace:dh`) ; (b) **récap de boucle** par ligne principale et conduite (`dhLoop`) : soudures dans l'ordre du fil étamé, PK, état des fils, longueurs électriques cumulées étamé / nu, **Ω attendus au départ si pontage à ce manchon** (ρ × (étamé + nu)), antennes en série (« ↳ », insérées au droit du té, aller-retour compté sur le fil qui les emprunte — après une inversion c'est l'autre fil), fin actuelle de la boucle surlignée (dernier manchon raccordé en continu depuis le départ), lignes grisées au-delà, encart « au départ, ponté à S-xxxx : x Ω, réflectomètre y m » ; (c) **mesure en un point** (clic sur une ligne du récap ou bouton « 📏 DH : mesurer depuis ce manchon » dans la fiche soudure) : distances amont / aval, Ω si pontage à l'autre bout, isolement attendu, saisie de la boucle mesurée → écart % et en mètres de fil, manchons suspects ; (d) localisation réflectomètre conservée ; ancien schéma filaire replié en bas. `setupTraceurLine` garde `parentM` ; `linkTraceurBranches` pose `branchLine` sur le té parent (utilisé par `wirePath` et `dhLoop`). Test `test/e2e_dh.mjs`.
- **18/08 — fils d'alarme aux tés et câblage virtuel (Ethan : « selon les fournisseurs ce n'est jamais pareil : LOGSTOR = le blanc (étamé) va dans l'antenne (catalogue p. 75 : l'étamé passe dans le branchement, le cuivre traverse la base) ; AXIOM / Renalia pas l'info ; certains ont 3 ou 4 fils sur les tés selon le DN, c'est flou ; il faut pouvoir choisir si on boucle l'antenne sur elle-même ou si on la raccorde au té ; relier les fils à la main virtuellement pour ne pas se tromper, photo à l'appui ; mini animation de la rotation ; retourner un té à saut haut/bas »)** : (a) paramètre DH **« fil dans l'antenne »** (étamé par défaut = LOGSTOR ; nu) + par antenne, sur la fiche de sa **soudure de sortie de té** : mode **en série** / **bouclée sur elle-même au té** / **pas encore raccordée**, et fil emprunté (défaut = réglage chantier) — champ `j.tee` synchronisé (`sync.saveWeld data.tee`) ; `antennaMode()` utilisé par `dhLoop` et `wirePath` ; les antennes bouclées sur elles-mêmes deviennent des boucles à part dans le sélecteur DH (« ↳ A2 (bouclée sur elle-même au té) ») ; (b) **câblage virtuel** dans « Déclarer manchonnée » (`wiringSVG`) : les deux bouts de tube en coupe vue depuis l'amont, fils à leur position horaire (rotation / retournement de la pièce pris en compte), on touche un fil amont puis le fil aval → liaison dessinée (rouge si inversion), « ∅ non raccordé », boutons tourner / retourner le tube aval ; les sélecteurs texte restent en dessous, synchronisés ; câblage rappelé en lecture seule dans la fiche une fois déclaré ; photo obligatoire déjà en place ; (c) **rotation animée** de la coupe (fiche pièce, groupe `#dialw`, .35 s) ; (d) **té à saut : sens du saut** (▲ au-dessus / ▼ en dessous, `e.sautDir`) sur la fiche pièce. Limites connues : orientation des pièces (rot/flip/sautDir) encore locale à l'appareil (pas dans line_state) ; câblage visuel du té (3 bouts) pas encore fait ; tés à 3 ou 4 fils : en attente d'infos fournisseurs. Tests `test/e2e_dh.mjs`, `test/e2e_wiring.mjs`.
- **18/08 — incident « je n'ai plus rien » (Ethan, Saint-Lô zone bleue).** Chaîne : (1) mon `pushHandoffs(remote)` du matin **effaçait la copie locale** (IndexedDB + localStorage) d'un chantier déjà envoyé qui n'était pas dans la liste renvoyée par le serveur — or `loadSites()` renvoyait `[]` en cas d'erreur/délai (gros chantier) → indiscernable de « tout supprimé » ; (2) au rechargement, plus de copie locale → l'appli démarre sur le chantier vide `__vide`, puis la liste serveur arrive et retire l'option `__vide` : le menu **affiche** « Saint Lô Zone Bleue » (première option) alors que le chantier ouvert est le vide → 0 soudure, bouton « Traceur : **créer** un réseau » ; (3) Ethan clique ce bouton → `traceur.html?site=new` → le traceur **se vide sans confirmation** ; (4) il retrace à 0,05 px/m (vue cadrée sur tout le fond DXF de plusieurs km) → ligne de 32 km, « calepinage dégueu ». Le réseau était intact **sur le serveur** (l'option de la liste vient de là) : récupération = re-choisir le chantier dans le menu, puis « ✎ Traceur : modifier ce réseau » (rechargement depuis le serveur), ⤓ JSON. Correctifs : `sync.loadSites()` renvoie **null** (jamais `[]`) hors ligne / erreur / délai (20 s) ; un chantier envoyé absent d'une liste serveur **valide** est seulement **masqué** (`hiddenAt`), plus jamais effacé localement ; après chargement des chantiers serveur, si on est sur `__vide` (ou un chantier disparu) → bascule sur le dernier ouvert / le premier, et le menu est resynchronisé sur le chantier réellement ouvert ; traceur `?site=new` : **confirmation** s'il contient déjà un réseau (avec rappel « ⤓ JSON ») ; nouveau réseau : vue à 10 px/m au centre du fond DXF (plus de cadrage sur des km) ; **tracé refusé sous 0,8 px/m** avec message « zoome avant de tracer ». Leçon : jamais de suppression locale automatique ; les listes serveur en échec ne sont pas des listes vides.
- **18/08 — « sur mon téléphone j'ai toujours des chantiers que je n'ai plus sur l'ordinateur »** : la suppression n'était pas propagée (ligne serveur parfois non supprimable par RLS, copies locales du téléphone jamais marquées, démos masquées appareil par appareil). Désormais **suppression = pierre tombale sur le serveur** (`sync.deleteSite(id,name)` : soudures / états / événements effacés, ligne `sites` conservée avec `data.deleted=true, deletedAt`) ; à la connexion chaque appareil lit les tombales → masque le chantier (`hiddenAt`), retire l'option, ne renvoie **jamais** sa copie locale (sauf si elle a été ré-enregistrée depuis le traceur après la suppression : `traceur.savedAt > deletedAt` → elle repart au serveur et le chantier revit) ; les démos intégrées (`bain`, `saintlo_jaune`) supprimées sur un appareil disparaissent partout (tombale sur leur id) ; au démarrage, les ids de `hiddenAt` sont aussi retirés de la liste intégrée. Un chantier supprimé peut toujours être recréé depuis le traceur (⤒ JSON ou réseau encore présent).
- **18/08 — coquille Saint-Lô : « plusieurs tés sont décalés, ça ne ressemble à rien »** : la branche des tés automatiques partait toujours du côté +1 de la parente — `side` mémorisé au clic de tracé vaut 0 → défaut +1 dans `traceur.js` (`nd.side||1`). Sur les maquettes de test l'antenne partait « du bon côté », sur un vrai réseau la moitié partent de l'autre côté : branche à l'envers, port du mauvais côté de la parente, l'antenne (re-rootée au port) traverse la parente en diagonale, soudures flottantes. Correctif : le spécial auto porte `aim` = le point de l'antenne à 1 m (`rebuild`), transmis au nœud (`extra.aim`), et la branche choisit son côté géométriquement (signe du produit scalaire normal·(aim−centre)) ; `side` reste le repli des tés d'attente posés à la main. Rappel de la même erreur deux fois : un commentaire `//` ajouté en fin de ligne compactée avale la suite → toujours en ligne propre + `node --check`. Test `test/maq_teeside.mjs` (antennes vers le haut, vers le bas, sur tronçon en biais : branche du bon côté, port collé à la conduite d'antenne, capture). **Pour corriger un chantier existant : l'ouvrir dans le traceur (✎) et « Enregistrer dans TRACÉ » (mise à jour) — numéros conservés.**
- **18/08 — serveur / comptes / synchronisation (demande d'Ethan : « creuser le côté serveur, utilisateurs, que le téléphone et le PC voient la même chose »)** : (a) **temps réel** : `sync.subscribeSite(id,{onWeld,onSite})` (Supabase Realtime, publication `supabase_realtime` sur `welds` + `sites` — activée par le script SQL) — le chantier ouvert reçoit en direct les statuts de soudure des autres appareils (appliqués soudure par soudure) et détecte un plan ré-enregistré ailleurs → `refreshSiteFromServer(id)` : recharge, remplace la copie locale ET le cache IndexedDB (`trace:handoff`, marqué `sent`), re-render en gardant zoom/onglet, toast « Plan mis à jour depuis un autre appareil » ; garde anti-écho 15 s après nos propres envois (`state.ownSiteWrite`) ; **filet sans temps réel** : toutes les 90 s (onglet visible) `pullRemote` + `listSiteMeta` (id, nom, updated_at — léger) et comparaison `localUpdatedOf` (updated_at serveur > savedAt traceur) ; `loadSite`/`loadSites` renvoient `updated_at` ; (b) **comptes réels** : `tools/supabase_setup.sql` (à coller dans Supabase → SQL Editor, ré-exécutable) — table `profiles` (rôle soudeur/manchonneur/chef/bureau, `active`), profil auto-créé INACTIF à l'inscription (trigger), Ethan = chef actif, fonctions `is_active()` / `my_role()` / `admin_set_user()` / `set_my_name()`, **RLS refaites** : lecture = comptes actifs seulement (un inscrit non activé ne voit RIEN → les inscriptions peuvent rester ouvertes), plans réservés chef/bureau (suppression chef), soudures = tout actif (suppression chef), line_state chef/bureau, events insertion seule, photos (storage) actifs, temps réel activé ; (c) **appli** : `sync.profile()/listProfiles()/adminSetUser()/setMyName()`, `me()`/`role()` = profil serveur quand connecté (entrée « moi » en tête du sélecteur, personnages de démo conservés hors connexion), bandeau « compte en attente d'activation », bouton **👥 Comptes** (chef) : liste des profils, rôle + actif modifiables en direct. Sans le script SQL, tout marche comme avant (profils absents → mode démo). Reste à faire ensuite : signature `by` des événements avec le vrai compte partout, historique des versions de plan (table `site_versions`), verrou d'édition simultanée du traceur.
- **18/08 — téléphone : « rien ne s'affiche en dessous de 2 px/m » (Saint-Lô zone bleue)** : sous 2 px/m l'appli dessinait le fond DXF réduit **entier** (jusqu'à 200 000 points dans un seul innerHTML) — le PC encaisse, le téléphone ne peint pas la couche → page blanche, réseau compris (au-dessus de 2 px/m, rendu fenêtré → OK ; le seuil donné par Ethan a mis sur la piste). Correctif : `decimateDrawing(drawing,{cap:45000,simp:0.5,minLen:1.5})` (dxfimport) — réseau gardé en entier, contexte simplifié à 0,5 m (invisible à ce zoom), plus courts que 1,5 m ignorés, plafonné ; utilisé en vue éloignée par l'appli (`bgFull`, cache `sh.drawingFar` si > 60 000 points) et par le traceur (`renderBg` vue entière, cache `b._far`, invalidé par `setBg`/`applyBackground`/`loadBgIDB`). Testé : fond synthétique 186 000 points sur écran téléphone → 5 155 traits dessinés en 430 ms, réseau visible, vue zoomée inchangée.
- **18/08 (soir) — « mon réseau a encore bougé » + téléphone toujours blanc en vue éloignée** : (1) la **vieille copie du téléphone a écrasé le serveur** — les copies locales d'avant l'introduction du marqueur `sent` passaient au travers du garde (`if(srv&&net.sent&&…)`) → à la connexion du téléphone, sa version périmée de Saint-Lô a écrasé la version réparée, et la synchro l'a redescendue sur le PC. Verrou : dans `pushHandoffs`, si le serveur a une version d'âge égal ou plus récent (`savedAt` traceur, sinon `updated_at`), on n'envoie **jamais** (marqué `sent`, on passe) — seule une copie locale strictement plus récente (> 1 s) part au serveur. Récupération : le traceur du PC garde son propre état (`trace:maquette:v1`) → l'ouvrir SANS `?site=` et ré-enregistrer ; (2) téléphone : en plus du fond entier, `drawingSVG` groupait tous les traits d'une même couleur en **un seul `<path>`** (jusqu'à 45 000 points après allègement, 200 000 avant) — c'est le même mécanisme que « le tracé disparaît au zoom » : au-delà de quelques milliers de points, le téléphone ne peint plus l'élément → découpage en paquets de ~3 500 points par `<path>` (vérifié : 186 000 points → 14 chemins, max 3 104 points). Note test : `e2e_bridge` « réapparu » demande ~3 s après `svGo` (démarrage plus lourd), délai porté à 3 000 ms.
- **18/08 (nuit) — Ethan réfute mon analyse « le téléphone a écrasé le serveur » : il a raison** (sa copie téléphone datait du matin, où le réseau était bon — l'écrasement aurait REMIS la bonne version). Vraies causes, visibles sur ses captures (« 1 m ≈ 0 px », deux traits de dizaines de km) : (1) **la ligne accidentelle L37 (~32 km, tracée pendant l'incident du zoom 0,05 px) a été enregistrée dans le chantier** → le cadrage s'étale sur 32 km, tout le vrai réseau devient sub-pixel → « rien ne s'affiche », sur téléphone comme sur PC (le « blanc sous 2 px/m » venait surtout de là — l'allègement du fond et le découpage des chemins restent des vrais correctifs, mais secondaires) ; (2) **les antennes glissaient à chaque retouche de la parente** : elles étaient accrochées à un CHAÎNAGE (`parent.m`) — retoucher la parente en amont change les chaînages → toutes les antennes aval (et leurs tés) glissent. Correctif : à chaque `rebuild`, le point d'accroche actuel est **reprojeté** sur le nouvel axe de la parente et `parent.m` mis à jour (tolérance 30 m ; au-delà, déplacement volontaire de toute la parente → on garde le chaînage) — une antenne est accrochée à un endroit, pas à un kilomètre-point (testé : sommet amont reculé de 20 m → antenne immobile, m 60→80, té au droit). Garde-fou : à l'enregistrement, toute ligne > 5 km est nommée avec sa longueur et demande confirmation (« tracé accidentel ? »). Nettoyage du chantier corrompu : traceur → onglet Lignes → supprimer les lignes aberrantes → vérifier les tés → Enregistrer. Leçon : quand Ethan conteste un diagnostic, réexaminer les faits AVANT de défendre l'explication — la sienne était la bonne.
- **18/08 (nuit) — validation d'Ethan : synchro temps réel OK (photo + statut téléphone → PC), réseau réparé** ; les tés ont bougé une dernière fois (ses réparations manuelles compensaient l'ancien défaut, la reconstruction corrigée les a recalées). **Historique de versions** ajouté en filet ultime : `tools/supabase_versions.sql` (table `site_versions` + trigger `before update on sites` → l'ancienne version part dans l'historique, 15 gardées par chantier, lecture comptes actifs, purge chef) ; appli : bouton **⏱ Versions** (chef/bureau, à côté de 🗑) → liste datée avec l'auteur → **Restaurer** (confirm ; `traceur.savedAt` re-daté pour gagner sur toutes les copies ; l'état actuel passe à son tour dans l'historique ; les statuts de soudure ne bougent pas, rattachés aux numéros) ; `sync.listVersions/loadVersion`. À faire par Ethan : coller `supabase_versions.sql` dans le SQL Editor une fois.
- **19/08 — correction des erreurs de saisie (demande d'Ethan : « supprimer les statuts d'une soudure, et transférer sur une autre si l'opérateur s'est trompé — choisir la cible sur le plan de préférence »)** : fiche soudure → (a) **« ↪ Transférer vers une autre soudure »** (visible dès qu'il y a des données ; tous rôles sauf bureau… en fait tous ceux qui voient la fiche) : la fiche se ferme, bandeau noir en haut du plan « Données de S-xxxx → touche la soudure cible » avec champ « ou n° » + OK + ✕, la source pulse en orange (`.tpulse`), on touche la soudure cible sur le plan (ou n° normalisé 3/4 chiffres via `findWeld`) → **déplacement** (statut, événements, photos, fils/conn, continuité, isolement, note — les champs structurels fc/dev/sortie de té/manchette restent) et la source repasse « À souder » ; si la cible a déjà des données → confirmation puis **échange** (rien ne se perd) ; événement `transfert` (avec « de S-xxxx ») journalisé sur les deux + `sync.logEvent` ; les deux soudures poussées au serveur (`pushWeld`) → synchro temps réel des autres appareils ; Échap ou ✕ annule ; ouverture de la fiche cible à la fin ; (b) **« ⌫ Remettre à “À souder” »** (chef uniquement, rouge, confirm) : `wipeWeld` (statut, events, photos, fils, tests, note) + event `remise_a_zero` au journal serveur. Piège trouvé : `canvas.setPointerCapture` au pointerdown **capturait aussi les clics des boutons posés dans le canvas** (le clic « OK » du bandeau n'arrivait jamais) → `#transferBar` ajouté à la liste d'exclusion du pointerdown (comme .zoomctl/.legend/.disp). Test `test/e2e_transfer.mjs` (transfert par tap plan, par n°, échange, remise à zéro chef).
- **19/08 — « Déclarer manchonnée » en VUE DE PROFIL + verrou d'orientation (demande d'Ethan : « les tubes sont présentés de face, il faudrait qu'ils le soient de profil… et une fois une soudure manchonnée à un bout d'un tube, l'autre extrémité ne doit plus être modifiable »)** : (a) `wiringSVG` réécrite (même signature, mêmes `data-wire`) : les deux bouts de tube **vus de profil** — gaine (mousse jaune + peau PE noire + face de coupe), acier dénudé face à face, cordon de soudure orange au centre, gabarit du manchon en pointillé ; les fils sont dessinés **par transparence** dans la mousse à leur hauteur horaire (`yWire` = −cos×20 − sin×2,5 pour séparer deux fils à même hauteur ; pointillé plus clair = fil qui passe derrière) puis **sortent de la mousse et s'écartent** vers deux bornes fixes (haut/bas — le plus haut dans la gaine part en haut) où on les touche pour les relier (pastilles É/N + heure, liaisons courbes, rouge si inversion, ∅ non raccordé inchangé). Boutons de rotation **pour les deux tubes** (amont `data-rota`/`data-flipa` nouveaux, aval `data-rotb`/`data-flipb`). (b) **Verrou** : `elLock(l,c,i)` → une pièce dont un manchon voisin (`joints[i-1]` ou `joints[i]`) est `manchonnee` n'est plus orientable — logique métier : si ce manchon-là est bon, le tube est forcément en bonne position. Dans `formManchon` : boutons remplacés par une `.lockbox` « 🔒 Pxx : position figée — déjà manchonnée en S-xxxx » ; dans `elView` : boutons ⟲⟳/retournée/0°/té à saut remplacés par la lockbox (mention « le chef remet d'abord ce manchon à “À souder” » — la remise à zéro chef du 19/08 est la porte de sortie) ; garde-fous aussi dans le handler de clics (toast). CSS `.lockbox` (index.html). Test `e2e_wiring.mjs` réécrit (profil cliquable, inversion, rot amont+aval, verrou fiche pièce + verrou formulaire + pièce libre témoin).
- **19/08 (suite) — vue du manchon redessinée (V2.3, validée sur maquettes HTML avant code — méthode demandée par Ethan : « on part sur des minis maquettes de présentation et je te dis ce que je préfère avant de toucher le code »)** : 4 pistes montrées (face→profil, biais léger/marqué, sombre, cadran), puis 3 itérations sur photos de chantier fournies (fils réels = TENDUS le long de l'acier, écarteurs plastiques, raccords sertis). Design retenu : tubes vus de côté, coupes DROITES, l'œil un poil au-dessus (plongée PL=8 : le fil de devant apparaît plus bas que celui de derrière — jamais superposés) ; chaque fil sort de la mousse à son heure réelle (`clockPos`) et file TENDU au plus court vers la sortie d'en face ; acier + cordon estompés (op .5) pour que les fils dominent ; raccords sertis au milieu, inclinés selon la pente, décalés du cordon ; en inversion la couleur change AU raccord + halo rouge + badge ⚠ INVERSION ; écarteurs suggérés (op .1/.26) ; heures près des sorties avec halo blanc (`paint-order:stroke`) ; étiquettes Pxx · amont/aval. Zones tactiles : cercles r22 sur les 4 sorties + moitiés de fil cliquables (paths transparents SÉPARÉS des cercles — un segment dans le même groupe gonflait le bbox et faisait rater les clics Playwright). `wiringSVG` réécrite (même signature/mêmes `data-wire`, viewBox 640×260, max-width 560). Rejets à retenir : arches au-dessus du tube (« les fils prendront le chemin le plus court »), fil-haut/fil-bas artificiel, tranches inclinées « coupe horrible » (la plongée doit venir de la caméra, pas d'un biseau du tube), ovale orange « schéma de CP », pointillé manchon.
- **19/08 (suite) — pas de 15° au manchon** (demande d'Ethan : « pouvoir pivoter plus légèrement tous les 15° ») : formulaire manchon → une ligne compacte par tube `Pxx amont · 30°  ⟲90 ⟲15 15⟳ 90⟳ ⇄` (`data-rota/rotb` = ±15/±90, `data-flipa/flipb`), angle courant affiché, verrou inchangé. Test e2e_wiring étendu (15/75).
- **19/08 (suite) — les fils SUIVENT L'ACIER (retour d'Ethan sur un tube retourné : « le fil blanc traverse l'acier et croise le orange alors que pas du tout en réalité »)** : `wiringSVG` → chaque fil est une **hélice sur l'acier** (`helix()`) de sa sortie à la sortie d'en face par le **chemin angulaire le plus court** (±180°), projetée avec l'œil un poil au-dessus (`yOf(g)=cy−rr·cos g+PL·sin g`, élévation `PHI=asin(PL/rr)`) ; `behind(g)` = sin(g+PHI)<0 et |dy|<Ra−2 → la portion qui passe derrière l'acier (≈ 7 h 30 → 9 h 45) est dessinée en dessous, estompée (`<g opacity=.62>`) sous un **voile d'acier translucide** (`rect` opacité .3 + masque `wpmk` fondu aux deux bouts ; acier de base ramené à .42) ; les portions devant restent nettes par-dessus. Déphasage (`ph`) : l'étamé fait sa torsion dans les 78 % premiers, le nu dans les 78 % derniers → deux fils qui font le tour ne se superposent pas (un seul croisement franc quand un tube est retourné). Raccords sertis, pattes d'écarteurs, recoloration d'inversion et zones tactiles (moitiés de fil) suivent la même hélice (visibilité héritée du segment). Vérifié visuellement : 5 h→10 h passe dessous puis derrière (cas d'Ethan), 2 h→5 h descend le long de l'avant, 180° = dessus-devant / devant-dessous-derrière. Tests e2e_wiring inchangés (verts).
- **19/08 (suite) — sens avant/arrière des tranches corrigé** (retour d'Ethan : « le orange est dessiné comme s'il passait devant mais il passe derrière, logiquement le chemin est plus court ») : chaque tranche est vue DEPUIS LE MANCHON → la moitié de l'ellipse côté manchon est l'ARRIÈRE du tube (7 h–11 h), la moitié côté gaine l'AVANT (1 h–5 h) ; c'était inversé (`exit()` : `x = xg − dir·sin(g)·kxm·.9`, un signe). Conséquence : les fils avant sortent côté gaine et traversent la tranche vers le manchon, les fils arrière sortent côté manchon ; la lecture d'Ethan (côté manchon = derrière) était la bonne.
- **19/08 — GÉOLOCALISATION, phase 1 (demande d'Ethan : « rattacher le réseau à un fond Google Earth, le point de l'opérateur, coordonnées GPS des photos pour qu'il n'y ait pas de triche »)** : nouveau module `src/geo.js` (aucune dépendance) : Lambert 93 + Lambert CC42→CC50 sur GRS80 (fwd/inv, validé Paris L93 ±0,3 m), Web Mercator, `detectCRS(X,Y)` (CC si X ≥ 1,0 M… sinon L93, contrôle lon/lat en France, préférence CC), `geoOfSite(net)` → affine plan→CRS depuis **l'origine DXF conservée** (`net.traceur.bgOrigin` des sites traceur, ou `net.origin` ajouté aux imports DXF de l'appli — v2 et JBTP) ou `net.geo` (calage enregistré, phase 3), `tilesFor()` (niveau z d'après px/m, tuiles de la fenêtre, **matrice tuiles→plan ajustée au centre de l'écran** : écart < 10 cm à 250 m, plafond 90 tuiles sinon z−1), `distLL`, `fmtDist`. **Constat clé : les 3 plans réels sont déjà géoréférencés** — Bain = CC48 (X≈1 349 500, Y≈7 193 200 → 47,843 N / −1,686 E), Saint-Lô = CC49, Indre = CC47 → calage automatique, rien à faire. Fond IGN Géoplateforme WMTS (`data.geopf.fr/wmts`, grille PM, sans clé, Licence Ouverte, mention « © IGN ») : `ORTHOIMAGERY.ORTHOPHOTOS` (jpeg), `GEOGRAPHICALGRIDSYSTEMS.PLANIGNV2`, `CADASTRALPARCELS.PARCELLAIRE_EXPRESS` (png, superposé .85), z max 19. Google Earth/Maps écarté : interdit hors Map Tiles API payante. Appli : `<g id="map">` sous `#bg` (papier du fond masqué via `#bg.nopaper` quand une carte est active), `renderMap()` à chaque rendu (innerHTML seulement si l'ensemble de tuiles change, sinon juste la matrice), panneau 👁 → section « Carte (IGN) » (Aucune / Photo aérienne / Plan IGN + case Cadastre, `state.show.carte/cadastre` mémorisés) avec le système détecté ou « plan non géoréférencé » ; crédit © IGN en haut à gauche. **GPS** : bouton ◎ (zoomctl) → `watchPosition` (haute précision), point bleu + cercle de précision dans `<g id="gps">` (`renderGps()`), recentrage au premier fix, arrêt au 2e appui ou quand l'onglet passe en arrière-plan, messages si refusé / introuvable / à plus de 20 km du plan ; `curPos()` (fix < 3 min) **attaché à chaque déclaration** (`ev.pos={lat,lon,acc,at}` sur soudée/manchonnée/contrôle/problème → part au serveur dans events) ; fiche → historique : « 📍 sur place (± 8 m) » ou **« 📍 déclarée à 1,5 km de la soudure »** en rouge si écart > max(80 m, 3×précision) — jamais bloquant. Tests : `test/geo_test.mjs` (26 ✓), `test/e2e_geo.mjs` (géoloc simulée Playwright : tuiles qui couvrent Bain, couches, zoom, point GPS exactement à (40,50), déclaration sur place / lointaine, arrêt). Le bac à sable n'a pas accès à data.geopf.fr → le rendu réel des tuiles est à valider par Ethan (réseau sur les rues). Reste : phase 2 filtre chef « déclarations loin de la soudure », phase 3 calage 2 points + recherche d'adresse pour les réseaux sans DXF, plus tard tracer sur la photo dans le traceur, pointages (volet interne, CNIL).
- **19/08 — GÉOLOCALISATION, phase 3 : calage à la main (Ethan : « on travaille le scénario sans DXF, plan décalqué à main levée »)** : pour tout chantier sans géoréf auto (traceur sans DXF, DXF en coordonnées locales, feuille PDF/image) — 👁 → « 📍 Caler sur la carte » (chef/bureau). Mode `state.calage` : repère 1 **sur le plan** (tap n'importe où, pas seulement une soudure) → bascule en **mode carte** (le canvas montre les tuiles IGN dans un repère Web Mercator `EPSG:3857` temporaire `C.mapGeo`, réseau/fond/marqueurs masqués, réticule au centre, pins rouges numérotées) centrée par géocodage du nom du chantier (texte avant le tiret) sinon centre de la France ; barre `#calageBar` : adresse / commune / « 47.84, -1.68 » (`geocode()` : IGN Géoplateforme puis BAN, ou coordonnées tapées), bouton 🗺 photo/plan, ✕ ; tap sur la carte = même endroit → repère 2 plan → carte → `similarityFromPairs` (similitude plan→**Lambert 93** avec retournement y ; 1 repère = translation seule « plan à l'échelle, nord en haut », bouton « Terminer avec 1 repère ») → confirm avec **contrôle : échelle ×1,00 ✓ / ⚠ si > 10 %, rotation du nord, distance des repères** → `net.geo={crs,aff,pts,by,at,s,rot}` → `saveGeo` (NET + SITES + copie handoff kv + `sync.saveSite`, `ownSiteWrite`) et carte photo activée. « Recaler » / « Oublier le calage » dans le panneau. **Le traceur conserve `geo`** d'une version à l'autre (siteRef.geo → bridge `site.geo=prev.geo`) : une retouche du tracé ne perd pas le calage (même repère de plan). Échap/✕ annule et restaure la vue plan ; changement de chantier = annulation. Tests : `geo_test.mjs` 33 ✓ (similitude exacte sur 2 repères, 1 repère, 3857), `e2e_calage.mjs` (parcours complet : écart repères 0,000 m, échelle 1,00, rot −3,4° = convergence, GPS au bon endroit, oubli, annulation). Exposé pour tests : `TRACE.siteGeo/startCalage/calageTap/geo/net`.
- **19/08 — « au-dessus de 3 px/m le fond photo/IGN disparaît » (Ethan)** : reproduit en Chromium headless — au-delà de ~2^25 px de coordonnée, Chrome **ne dessine plus les `<image>` SVG** (tuiles z18 à 6,7·10^7 px, z19 à 1,3·10^8 px ; float32 dans Skia). Correctif `renderMap` : tuiles en **coordonnées locales** (première tuile de la fenêtre à 0,0) et origine repliée dans la matrice (`e' = e + a·ox + c·oy`, `f' = f + b·ox + d·oy`). Test e2e_geo : tuiles IGN servies localement (`page.route` → tuile verte) et **pixel vérifié peint** à 27 px/m (z19). Piège générique à retenir pour tout SVG : jamais de coordonnées > 10^7 dans les éléments, toujours relatives + transform.
- **19/08 — ◎ sur téléphone : « localisation pas autorisée » sans demande (Ethan)** : un navigateur qui a mémorisé un refus (ou Safari/Chrome sans droit de localisation dans les réglages) répond PERMISSION_DENIED (code 1) **sans jamais redemander** — aucun code ne peut forcer la demande. Fait : (1) `getCurrentPosition` appelé **directement dans le geste** (c'est lui qui déclenche la demande quand elle est possible) puis `watchPosition` ; (2) sur code 1 → **modale « Localisation refusée »** avec la marche à suivre selon l'appareil (iPhone Safari / écran d'accueil : Réglages → Confidentialité → Service de localisation → Safari ou l'app → Lors de l'utilisation, ou AA → Réglages du site web → Position ; Android Chrome : cadenas → Autorisations → Position ; PC : icône à gauche de l'adresse) + bouton **Réessayer** (relance dans un geste) ; anti-doublon 2 s (deux erreurs arrivent : getCurrentPosition + watch) ; codes 2/3 → toast. Test `e2e_gpsdeny.mjs` (stub géoloc qui refuse → modale → réautorisation → Réessayer → point affiché).
- **19/08 — photos compressées côté téléphone** (`compressPhoto` : `createImageBitmap` avec orientation EXIF, sinon `<img>`, sinon brut ; 1600 px de grand côté, JPEG 78 %) : 8,2 Mo (pire cas texture gravier) → 500 Ko, photo réelle ≈ 200-350 Ko, soit 10-15 × moins de stockage/trafic Supabase ; toast « Photo ajoutée (320 Ko) ». Test `e2e_photo.mjs` (setInputFiles 4000×3000 → ≤1600 px, < 900 Ko, jpeg). **Coûts d'hébergement (calculé le 19/08 pour Ethan, scénario 50 chantiers / 30 utilisateurs / 20 chantiers actifs × 12 déclarations/jour)** : GitHub Pages 0 €, IGN 0 €, Supabase Pro 25 $/mois (photos ≈ 2,3 Go/mois → 100 Go inclus pour ~3,5 ans, trafic ≈ 10 Go/mois sur 250 inclus, base < 8 Go) → **≈ 30-40 €/mois tout compris**. **Pré-requis AVANT de monter en charge** (pas faits) : (1) `loadSites()` charge aujourd'hui TOUS les chantiers avec leur `data` au démarrage → passer en chargement à la demande (`listSiteMeta` + `loadSite(id)` à l'ouverture, fond DXF en différé) — sinon 50 × 4 Mo = 200 Mo par lancement ; (2) `site_versions` copie le JSON complet avec le dessin DXF (15 × 4 Mo par chantier) → stocker les versions sans `drawing` ; (3) liens e-mail de connexion : le serveur mail intégré Supabase est limité à quelques envois/heure → brancher un SMTP (Office 365 SCR ou Resend) avant une vraie équipe ; (4) bouton « Exporter tout le chantier » (sauvegarde froide).
- **19/08 — ÉCRANS D'ENTRÉE + chargement à la demande (direction A validée sur maquettes : « Chantier clair »)** : plus aucun plan complet chargé au démarrage. (1) **Connexion** (`#loginView`, 1re visite sans session) : logo TRACÉ (2 tubes + point de soudure orange), lien e-mail (sync.login), « Continuer hors connexion » (localStorage `trace:skipLogin`) ; l'écran ne réapparaît plus une fois vu/connecté. (2) **Accueil « Mes chantiers »** (`#homeView`) : fusion `homeMetas()` = SITES locaux (clés du dict = ids ! les démos n'ont PAS de champ id — piège corrigé) + `state.serverMetas` (métas LÉGÈRES : `listSiteMeta` enrichie en sélections JSON PostgREST `geo:data->geo, origin:data->origin, bgo:data->traceur->bgOrigin, sat, w, h, deleted, builtin` — quelques Ko pour 50 chantiers au lieu de 200 Mo) ; vue **Carte de France** (contour stylisé `FRPTS`, pins à la position réelle via `llOfMeta`→`geoOfSite`+centre, couleur = avancement, bulle au tap + bouton Ouvrir, « non localisés » en cartes dessous) ↔ vue **Liste** (recherche, tris récents/A-Z/avancement, démos en bas avec badge, « ＋ Nouveau chantier » chef/bureau → traceur ?site=new ou import) ; carte **« Reprendre »** (trace:lastSite) ; bandeau compte inactif / hors connexion ; avatar → compte (nom, 👥 chef, déconnexion). (3) **Ouverture à la demande** `openSiteFromHome(id)` : local complet → switchSite direct ; sinon spinner + `sync.loadSite(id)` (un seul chantier) ; le select topbar passe par elle aussi ; bouton **⌂** dans la topbar pour revenir à l'accueil ; `?site=` (remise traceur) saute l'accueil (tests inchangés). (4) **onAuth réécrit** : plus de `loadSites()` complet — métas normalisées → `pushHandoffs(norm)` (réconciliation/tombstones inchangée), options du select, `state.homeStats=sync.siteStats()` (**RPC `tools/supabase_home.sql` À EXÉCUTER par Ethan** — sans elle : « avancement inconnu », rien ne casse). Ordre showScreen AVANT renderHome (sinon « Reprendre » invisible). Tests : `e2e_home.mjs` (login→skip→carte→liste→recherche→ouverture→⌂→Reprendre→rechargement→`TRACE.go()`) ; e2e_photo/e2e_demo patchés (`TRACE.go()` après goto) ; régressions bridge/geo/transfer/wiring/calage vertes. **MULTI-ENTREPRISES (à faire AVANT commercialisation, noté le 19/08 à la demande d'Ethan)** : aujourd'hui un compte inconnu = `active=false` → ne voit RIEN (déjà en place) ; pour plusieurs entreprises étanches : table `orgs`, `org_id` sur profiles/sites/welds/line_state/events/storage, RLS `org_id = my_org()` partout, activation par le chef DE l'org, invitations par domaine e-mail — migration SQL + petite passe appli (~1 séance) ; à ce moment-là passer Supabase en Pro et brancher un SMTP.
- **20/08 — ONGLET « HYDRO » (demande d'Ethan : « onglet hydraulique : voir tout le réseau, le sectoriser en tronçons, tableau récap linéaires et volumes par tronçon, surbrillance des points d'eau/bornes incendie, choix du point de remplissage → où mettre des by-pass, indicateur si un kit fin de ligne déjà soudé alors qu'un by-pass aurait été mieux, d'abord sélectionner les prestations, zone de remplissage avec encombrement des skids » ; maquette 3 écrans validée : « laisse ses données en variable, mais c'est parfait comme ça »)** : nouvel onglet entre DH et Liste. (1) **Module pur `src/hydro.js`** (testé en node, `test/hydro_test.mjs`, 35 ✓) : table Ø intérieurs P235GH DN20→400 (`DN_INT`, `areaDN` ; 1 m/s en DN100 = 32 m³/h comme la maquette), `needsFlow` (rinçage/passivation/remplissage ⇒ circulation ⇒ by-pass ; épreuve seule ⇒ statique ⇒ kits fin de ligne), `buildHydro(lines,{prest,params,cuts,fillAt})` → coupes valides (ignorées à < 0,5 m d'un bout ; « sur vanne » si à ± 0,3 m d'un élément valve), sous-segments par ligne, **composantes connexes** (union-find, l'antenne rejoint son parent au chaînage `parentM` — repris de `parentElIdx`/`e.branch` pour les imports) = **tronçons**, par tronçon : linéaire aller, **volume aller+retour** (Σ aire(DN)·longueur·nCond, prorata des éléments coupés, teeout/endpoint exclus, mono-conduite ×1), DN par longueur décroissante, extrémités typées (départ racine / bout de ligne / côté de coupe) avec besoin : `BP` si circulation, `KFL` sinon, `EVAC` si mono-tube en circulation, `none` si déjà bouclé (bout `bypass` = « ✓ bouclé », `endpoint` = « ✓ raccordé » SST — choix de la maquette validée), coupe sur vanne = vanne fermée (rien à poser), extrémité à ≤ 12 m du point de remplissage = bouclée par le skid ; **alerte** si bout en `endcap` dont un joint A/R est soudée/contrôlée/manchonnée alors que le besoin est BP (« bouchon déjà soudé (S-xxxx) : un by-pass aurait été préférable ») ; débit de rinçage = vitesse·aire(DNmax)·3600 ; durée de remplissage = volume/débit borne. (2) **Données** : `NET.hydro={prest,params,cuts:[{line,m}],water:[{x,y}],fill:{x,y}|null,skid:{x,y,rot}|null}`, `saveHydro()` calqué sur `saveGeo` (SITES + handoff kv + `sync.saveSite` débouncé 1,2 s) ; conservé par le traceur (`bridge.js prev.hydro→site.hydro`, `siteRef.hydro` dans main.js) comme le calage geo. **Paramètres en VARIABLES (exigence d'Ethan)** : vitesse de rinçage (déf. 1 m/s), débit borne/skid (30 m³/h), gabarit skid (3×8 m) — inputs dans l'onglet (details « ⚙ Paramètres »), `HYDRO_DEFAULTS` dans hydro.js. (3) **UI (`renderHydro`)** : cartes prestations cochables (badge « ⇄ by-pass requis » / « ✓ kits fin de ligne suffisants », encart jaune « poser les BP AVANT l'épreuve » si épreuve+circulation), boutons de pose ✂ Sectoriser / 💧 Point d'eau / 🚰 Remplissage / ⬛ Skid → basculent sur l'onglet Plan en mode pose (`state.hydroPose`, bandeau `#hydroBar` avec ↻ 15° pour le skid et « Terminer » ; tap dans `endPtr` comme calage/transfert ; re-tap sur un marqueur = suppression ; Échap = sortir ; réservé chef/bureau comme le calage), chips ✕ pour retirer coupes/bornes/remplissage/skid depuis l'onglet, **mini-plan SVG** fitté (axes gris + tronçons colorés `TCOLS`, ✂ avec étiquette « coupe : vanne V2 », bornes 💧 pulsantes nommées B1/B2…, pill « 🚰 REMPLISSAGE ICI » reliée en pointillé au point du réseau le plus proche, skid hachuré à l'échelle (m × ppm) et orientable, badges « ⇄ BP » noir/orange · « KFL » gris · « ⚠ soudé » rouge · « 🚰 remplissage » bleu ; si > 10 extrémités à équiper le mini-plan passe en pastilles seules pour rester lisible), même overlay dessiné sur le VRAI plan pendant la pose (`<g id="hydroG">` entre markers et gps, `renderHydroOverlay` dans `renderPlan`), cartes récap par tronçon (linéaire · DN, volume A+R, débit de rinçage ≥ v m/s (DNmax), durée de remplissage, à poser, lignes d'alerte rouges), total chantier, note « sans coupe, chaque réseau non relié forme déjà son propre tronçon » (les démos DXF ont plusieurs racines déconnectées → plusieurs tronçons d'office, c'est voulu), **rapport imprimable** (`openHydroReport` : page autonome via `window.open` + bouton Imprimer/PDF : prestations, paramètres, tableau tronçons, coupes, visa). `window.TRACE.hydro={of,build,pose,tap,end,save,nearest}` pour les tests. **Tests** : `hydro_test.mjs` (node, 35 ✓ : volumes, greffes amont/aval d'une coupe, vanne, remplissage, mono-tube, coupes invalides, paramètres) + `e2e_hydro.mjs` (onglet, coche rinçage → BP partout + débit ×1,5 quand vitesse 1,5, pose ✂→2 tronçons, 💧/🚰/skid dessinés, chips, alerte bouchon soudé en direct, suppression, persistance NET.hydro, soudeur = boutons désactivés) ; régressions home/geo/recap/dh/wiring/misc/transfer/zoom vertes. Au passage : **arrivée par `?site=` (remise traceur) pose `trace:skipLogin`** (avant : un rechargement ultérieur SANS ?site retombait sur l'écran de connexion — révélé par e2e_zoom, patché aussi : il rouvre son chantier via `TRACE.go(id)` après reload et vise (280,300) pour fermer le panneau 👁 devenu plus grand) ; `fmtVol` : volumes < 1 m³ en 2 décimales (avant : « 0 m³ » à cause de fmt 1 décimale).
- **20/08 — CARTE DES CHANTIERS = VRAIE CARTE IGN (retour d'Ethan : « la carte est dégueu, ça fait schéma ; il faut pouvoir zoomer, on peut avoir plusieurs chantiers dans la même ville » ; 3 styles maquettés — plan IGN couleur / plan éteint / ortho — direction B « plan éteint » validée)** : le contour SVG de la France (FRPTS/frProj, supprimés) est remplacé par une carte web mercator maison dans `initHomeMap` (~70 lignes, aucune dépendance) : tuiles PLANIGNV2 via `ignTileURL('plan',z,x,y)` passées en gris (`filter:grayscale(1) contrast(.88) opacity(.82)` sur `.hmTiles img`) pour que les pins ressortent, **zoom molette / double-clic / pincement (2 doigts, seuil ×1,35) / boutons + − ⌖**, glisser pour se déplacer, z entier 5→19, vue conservée dans `state.homeMapView` (le ⌖ refait le cadrage sur tous les chantiers ; re-render de l'accueil ne bouge pas la vue). **Pins** : pastille blanche cerclée à la couleur d'avancement (`pctColor`, ✓ à 100 %), étiquette nom dès z9, fiche au tap (nom, % manchonné, soudures, dernière activité, bouton Ouvrir → `openSiteFromHome`). **Regroupement** : pins à moins de 36 px → pastille noire « n » ; tap = zoom +2 centré ; à z19 les chantiers d'une même adresse s'écartent en éventail (rayon 24 px) au lieu de se grouper. Pins hors écran non rendus (± 80 px). **Piège résolu : `setPointerCapture` sur le conteneur retargette les `click` vers lui → les listeners des pins ne recevaient rien** ; les taps se gèrent au pointerup avec la cible mémorisée au pointerdown (`downTgt`), comme sur le canvas du plan. Boutons +/−/⌖ remontés à `bottom:78px` pour ne jamais passer sous la fiche. Crédit « © IGN — Géoplateforme » affiché (Licence Ouverte). Chantiers sans géoréf : toujours listés sous la carte (« non localisés ») ; carte vide = message « cale un plan sur la carte ». CSS `.homeMap/.hmPin/.hmCluster/.hmCard/.hmCtl/.hmLegend` dans index.html. `state.homePin` et la bulle SVG suppr. (la fiche interne remplace) ; branche `[data-pin]` du délégué `#homeBody` retirée. Maquette : `maquette_carte_accueil.html` (3 cartes interactives comparées). Tests : e2e_home adapté (tuiles/contrôles/crédit + zoom + fiche pin) ; calage vérifié au pixel via tuiles factices (`page.route` → col/row/offset attendus) ; scénario 2 chantiers à Saint-Lô + 1 à Bain : fit → pastille « 2 », 2 taps → séparation z14, fiche, drag sans fiche, ⌖. NB : les tuiles IGN ne chargent pas depuis le conteneur (réseau en liste blanche) — le rendu réel est validé chez Ethan, la mécanique par tuiles factices. Identité visuelle (nom, logo) : à retravailler plus tard (dit par Ethan).
- **20/08 (suite) — retours d'Ethan sur la carte accueil + hydro** : (1) **« les chantiers sont mal placés sur la carte »** : `llOfMeta` prenait le MILIEU DE LA FEUILLE [w/2,h/2] — pour un chantier dont le réseau est excentré dans son repère (DXF Lambert replié, feuille avec marges), le pin partait à des centaines de mètres/km. Fix : centre réel = moyenne des repères de calage `geo.pts` (posés SUR le réseau) > centre de `net.bbox` (bridge traceur : réseau + fond réduit ±150 m ; ajouté à `listSiteMeta` : `bbox:data->bbox`) > [w/2,h/2] en dernier recours. (2) **% à l'accueil = % SOUDÉ** (soudée+contrôlée+manchonnée), plus % manchonné : `tools/supabase_home.sql` **V2** (drop + recreate `site_stats()` avec colonne `soud` — **à ré-exécuter dans Supabase → SQL Editor**), `sync.siteStats` mappe `soud` avec repli sur `manch` tant que le SQL v1 tourne, `statOf` et les libellés passent à « % soudé ». (3) **Vue d'ensemble hydro refaite** (« dégueu, textes gros, pas de fond, pas zoomable ») : `hydroMiniSVG` (SVG statique fitté, badges scale 1/k_fit → géants) remplacée par **`initHydroMap`** : le vrai plan en petit — fond de plan léger en cache `hydroBgSVG(sh)` (DXF décimé opacité .5 via `drawingSVG`+`decimateDrawing`, ou image du traceur ; les démos sites.json n'ont pas de fond embarqué, les chantiers réels traceur oui), axes gris, overlay hydro, **pan/zoom continu** (molette exp, pincement ratio, double-tap ×2, boutons + − ⌖, bornes 0,02→60 px/m), transform SVG + overlay régénéré au rAF, vue conservée `state.hydroMapView` (reset au changement de chantier). (4) **Seuils de zoom dans `hydroOverlaySVG`** (mêmes règles sur le plan en mode pose et la vue d'ensemble, pilotées par kpm=k·ppm) : toujours visibles = ⚠ soudé, 🚰, ✂+étiquette, bornes, skid ; « ⇄ BP » en texte dès 0,35 px/m sinon pastille noire ; KFL/évac dès 0,6 ; « rien à poser » (✓ SST / ✓ bouclé / raccordement) seulement dès 1,2 px/m ; badges réduits (15 px de haut, police 9,5) ; l'heuristique « many » supprimée. Légende compacte si > 6 tronçons. (5) **Vocabulaire métier corrigé (Ethan : « sous-station ≠ raccordement »)** : bout d'antenne en endpoint = **« SST (nom) » / badge « ✓ SST »** (sous-station de bâtiment, bouclée chez elle) ; départ racine en endpoint = **« raccordement »** (piquage sur réseau existant / chaufferie), already 'racc' ; une coupe hors vanne = « raccordement entre tronçons à la remise en service (soudure de raccordement hors épreuve) » dans le rapport imprimable. Tests : hydro_test 37 ✓ (labels SST/raccordement ajoutés), e2e_hydro adapté (pastilles dézoomé + badge BP visible zoomé via centerOn), régressions home/geo/recap/wiring vertes. **⚠ Ethan doit ré-exécuter `tools/supabase_home.sql` (v2)** pour le % soudé — sinon l'accueil retombe sur le % manchonné.
- **20/08 (suite 2) — « toujours pas bon : Saint-Lô ne pointe pas à Saint-Lô, et le % doit être soudures finalisées / TOUTES les soudures du réseau »** : (1) **le pin restait faux** parce que `site.bbox` (bridge) est l'emprise réseau + FOND DXF réduit, et surtout parce qu'une seule ligne partie loin (tracé accidentel) ou un fond étendu suffit à déplacer un centre de bbox de dizaines de km. Fix en profondeur : `netCenterOf(net)` = **médiane des milieux de lignes** (formats couverts : `L.axis` traceur, `L.pts`, `L.els from/to`) — insensible aux aberrants ; ordre de priorité de `llOfMeta` : repères de calage `geo.pts` > **médiane des lignes (copie locale)** > **`nbox`** (nouveau : emprise du RÉSEAU SEUL, écrite par le bridge à chaque enregistrement, sélectionnée dans `listSiteMeta` via `nbox:data->nbox`) > `bbox` > milieu de feuille. Validé e2e : réseau à Saint-Lô + ligne à 40 km + bbox [0,0,80000,80000] → pin à ~50 m du vrai centre. Les chantiers existants d'Ethan : la copie locale donne la médiane tout de suite ; le `nbox` serveur arrivera au prochain enregistrement traceur. (2) **% de l'accueil** : la table `welds` ne contient QUE les soudures déjà déclarées → `site_stats` seul surestime énormément. Fix : total = **soudures du réseau entier** : `nw` = `data->report->welds` (méta serveur, sélection `listSiteMeta`) ou compté localement (`nwOf` : Σ `cond[A/R].welds|joints`) ; `HOME_NW` rempli par `homeMetas` ; `statOf` → `{total:max(nw,rpc.total), done:rpc.soud, pct:done/total}` plafonné à 100. Fiche pin : « 40 % soudé · 213/530 soudures ». Démos sans cond ni stats → « avancement inconnu » comme avant. `window.TRACE.sites` exposé (tests). Régressions e2e_home + hydro_test 37 ✓.
- **20/08 (suite 3) — HYDRO v2 : « un vrai onglet de prépa pour le conducteur de travaux » (demandes d'Ethan)** : (1) **Calendrier prévisionnel à pastilles** : `h.cal=[{op,t,d}]` — opérations `CAL_OPS` (hydro.js : eau brute 💧, épreuve 🧪, rinçage 🌊, vidange 🕳, eau adoucie 🚰, passivation ⚗️, autre 📌), posées **librement, répétables (2 remplissages eau brute possibles), dans l'ordre voulu, aux dates voulues**, par tronçon ou « tout le chantier » (t=-1) ; frise SVG `calFriseSVG` (une rangée par tronçon + Chantier, pastilles aux dates, axe des jours), chips éditables ✕, formulaire opération/tronçon/date ; tri chronologique stable à l'ajout ; ligne « Calendrier » dans chaque carte tronçon. (2) **Plusieurs zones de remplissage** `h.fills=[]` (« au moins une par tronçon ») et **plusieurs skids** `h.skids=[]` (↻ pivote le dernier posé) — migration auto fill/skid→fills/skids dans `hydroOf` ; `buildHydro({fills})` : chaque zone boucle son extrémité, `t.needFill`/`totals.noFill` = tronçon sans zone (encart jaune carte + total + dossier). (3) **Corrections métier d'Ethan (messages en cours de séance)** : un RACCORDEMENT (réseau existant / entre secteurs d'épreuve) n'est PAS soudé pendant l'épreuve → extrémité ouverte : **BP si circulation, KFL si épreuve seule** (fini le « rien à poser ») ; **en SST aussi le by-pass se pose** (on ne boucle pas à travers l'échangeur) → SST = besoin normal, label « SST (nom) » ; **« fond bombé déjà soudé »** remplace « bouchon déjà soudé » partout (cartes, légende, dossier). (4) **Bornes incendie de la ville** : bouton 🔎 → Overpass/OSM (`emergency=fire_hydrant`, bbox du chantier ± ~300 m, 2 serveurs en repli), converties en repère plan via le géoréférencement, affichées « BI » bleu (indicatives, non persistées) ; en mode 💧 un tap sur une BI la retient à sa position exacte (snap). Nécessite un chantier calé (toast sinon). NB : intestable depuis le conteneur (réseau restreint) — gestion d'erreur + toasts en place, à valider chez Ethan. (5) **Dimensionnement pompe de rinçage** (`t.pump`) : débit = v·aire(DNmax) ; **HMT = classe de DN la plus défavorable à la vitesse cible** (rinçage PAR PASSES, une branche à la fois — la 1re version sommait tout le débit dans chaque DN : HMT 41 738 m sur la démo, corrigé), Darcy-Blasius ν=1e-6, ×2 (A+R) ×1,2 (singularités) +3 m ; affiché « ≥ Q m³/h · HMT ≈ x m (ΔP bar) », note « à confirmer par le loueur » dans le dossier. (6) **Dossier de prépa** `openHydroReport` refait : **global + UNE PAGE PAR TRONÇON** (`.page` avec `page-break-before`) — global : prestations, params, calendrier (frise + tableau), vue d'ensemble carte (`hydroPrintMap` : fond `hydroBgSVG` + axes + overlay, viewBox fitté), synthèse par tronçon, coupes ; par tronçon : carte ZOOMÉE (`tronconBBox` sur ses segs +12 %), tableau chiffré, **liste de toutes les extrémités avec « à prévoir »** (`needTxt`), alertes fond bombé, étapes datées ; visa + réserves en dernière page. Testé Playwright via l'événement popup : 9 pages, 10 SVG, frise/extrémités/pompe présents, 0 erreur. Tests : hydro_test 43 ✓, e2e_hydro enrichi (2 zones → noFill décrémente, pompe bornée, calendrier trié + répétable + frise + chips, persistance fills/cal), régressions home/geo/recap vertes.
- **20/08 (suite 4) — CALENDRIER À GLISSER-DÉPOSER (retour d'Ethan : « la pastille seule on ne comprend pas, écrit direct avec une pastille propre, calendrier pro ; j'aimerais voir glisser » ; 2 maquettes interactives livrées — `maquette_calendrier.html`, planning par tronçon vs agenda mensuel, drag réel — verdict : A+B les deux vues + glisser ET formulaire)** : la frise SVG à emojis est remplacée par deux vues HTML sur les mêmes données `h.cal` : **« Planning »** (`calPlanHTML` : grille 14 jours × une rangée par tronçon + « Chantier », en-têtes jj/mm + jour de semaine, week-ends grisés, aujourd'hui marqué, nav ‹ › ± 7 j) et **« Mois »** (`calMoisHTML` : agenda 6 semaines, nav ‹ › mois, chips « poser sur : T1/T2/Chantier » = tronçon actif du drop). Étiquettes `calEvHTML` : **nom écrit** (« Épreuve », « Rinçage »…) sur fond couleur opération + **bande verticale couleur du tronçon**, title au survol. **Drag & drop pointer-events** (souris + doigt, `touch-action:none` sur pastilles/étiquettes) : palette (7 opérations) → jour = pose ; étiquette → autre jour = déplacement (en Planning la rangée cible change aussi le tronçon) ; étiquette → zone « 🗑 glisser ici pour enlever » = suppression ; ghost `position:fixed` sous le doigt, cellule survolée en surbrillance `.hot`, **auto-scroll du bandeau** quand le ghost approche des bords (± 44 px → scrollLeft ± 16, piège vu en test : cellule cible hors zone visible du wrap scrollable = drop dans le vide). Réservé chef/bureau (`hydroCanEdit`), formulaire opération/tronçon/date CONSERVÉ en secours + chips ✕. États : `state.hydroCalView` ('plan'|'mois'), `hydroCalStart`, `hydroCalMonth`, `hydroCalT` — reset au changement de chantier. **Dossier imprimable** : le calendrier y est la grille Planning en pleine période (`calPlanHTML(H,h,true)` : min(dates)→max, 7 à 42 jours), styles hyCal embarqués dans le HTML autonome. `calFriseSVG` supprimée. Tests : drag posé/déplacé/supprimé + auto-scroll (240 px) vérifiés par souris Playwright ; e2e_hydro (formulaire, tri chronologique, opération répétée ×2, persistance) et hydro_test 43 ✓ verts. Question d'Ethan sur la méthode : maquettes d'abord = moins cher que coder puis refaire — confirmé, on continue ainsi.
- **20/08 (suite 5) — retours d'Ethan sur le calendrier (« c'est parfait/génial » + retouches) et fond de carte** : (1) **Mini-calendrier de navigation** : le label de date (« Août 2026 » / « 20/08 → 02/09 ») est cliquable (📅, souligné pointillé) → `input type=date` invisible + `showPicker()` → saut direct à une date (Planning : la fenêtre démarre la veille ; Mois : le mois de la date). (2) **Vue Mois plus claire** : chaque étiquette porte un badge « T1 »/« CH » à la couleur du tronçon (`calEvHTML(...,showT)`, CSS `.hyEv .tn`) en plus de la bande. (3) **Émojis retirés** de la ligne « Calendrier » des cartes récap (pastille de couleur + nom court + date, séparées par →) et du dossier (pastilles HTML). (4) **Dossier PAR TRONÇON** : bouton « 📄 Dossier » dans l'en-tête de chaque carte récap → `openHydroReport(idx)` : entête + prestations/params + LA page du tronçon seule (sans vue globale/synthèse), 1re page sans saut (`tronPage(t,noBreak)`) — 1re version buggée (le corps HTML ignorait le filtre → global complet), corrigé par un corps conditionnel `only==null`. (5) **Planning par tronçon DANS le dossier global** : chaque page tronçon a sa grille `calPlanHTML(H,h,true,t.idx)` (ses étapes + celles « chantier ») au-dessus du tableau. (6) **Planning global du dossier : période complète COMPRESSÉE** (message d'Ethan : dates très espacées → « il prend pas tout en compte ; tout doit apparaître mais visuel, couper les semaines vides ») : ≤ 21 j = continu ; au-delà, colonnes = jours ± 1 autour de chaque étape et les trous deviennent des séparateurs hachurés « ⋯ +n j » (`cols=[{d}|{gap}]`, plus de cap arbitraire à 42 j qui TRONQUAIT). (7) **Fond des cartes hydro = PHOTO AÉRIENNE GRISÉE** (message d'Ethan : « pas dit qu'on ait des beaux DXF partout ; ortho simplifiée grisée, notre réseau mis en avant ») : si le chantier est géoréférencé, la vue d'ensemble (`initHydroMap` : groupe `#hyBgT` mis à jour au rAF, cache par clé de tuiles, matrice à origine repliée comme `renderMap`) et les cartes du dossier (`hydroTilesSVG` via `tilesFor`, ids de filtre uniques `hyGrayP<n>`) affichent les tuiles ORTHO IGN passées par un filtre SVG délavé (`HY_GRAY` : saturate 0.12 + slope 0.52/intercept 0.4) — axes en #8b8577, crédit « © IGN — Géoplateforme » affiché (vue + dossier) ; sans géoréf : repli sur le fond DXF/uni comme avant. `TRACE.geoRefresh()` exposé (invalide le cache géo — utile aux tests). Piège corrigé au passage : formule de matrice charabia dans la 1re écriture de `hydroTilesSVG` (e'/f' = e+a·ox+c·oy / f+b·ox+d·oy). Tests : ortho (4 tuiles+filtre+crédit, rechargées au zoom), dossier global (gaps ⋯, 7 « Planning du tronçon », 70 images ortho, © IGN), dossier tronçon seul (pas de sections globales), e2e_hydro + hydro_test 43 + home + geo verts. NB : `pop.screenshot` Playwright bloque sur « waiting for fonts » avec les popups `document.write` — sans impact appli.
- **20/08 (suite 6) — « la liste des étapes prend trop de place »** : sous la grille du calendrier, la liste exhaustive des chips (3 rangées dès ~15 étapes) et le formulaire sont repliés dans deux `<details>` fermés par défaut : « n étapes — liste détaillée (✕ pour enlever) » et « ＋ Ajouter par formulaire (sans glisser) » — une ligne chacun ; chips sans émoji (pastille couleur + nom court) ; suppression ✕ et ajout testés après ouverture des replis.
- **20/08 (suite 7) — DH COMPLÈTE, livraison A (maquette 4 écrans validée « c'est parfait ! » + règles métier d'Ethan)** : règles enregistrées : la valeur ohmique compte les DEUX fils (aller jusqu'au bouclage + retour — dhLoop le faisait déjà via dE+dN, confirmé) ; **fil dans la branche d'un té : LOGSTOR = étamé, AXIOM / Renalia = cuivré (nu)** (`wireDefFournisseur`, antWire passe à « auto (fournisseur) » avec forçage E/N possible) ; **té retourné vers le bas (vidange `vert:'down'`, té à saut passant dessous `sautDir:'bas'`) : cheminement INVERSÉ** (`wireOfTee(e)`) ; **4 fils : AXIOM dès DN300 compris, Renalia dès DN350 compris** (`nWiresOf`, badge « ⚠ 4 fils (2 paires) » dans l'onglet — câblage des tés 4 fils à intégrer quand Ethan aura les schémas fournisseurs). **Piquages purge / vidange dans la boucle** : le fil concerné y monte — état déclaré « bouclé dans la coiffe / sortie de coiffe » → + 2 × piqL (param, déf. 2 m) sur CE fil ; « fils en attente » → circuit COUPÉ là (lignes grisées au-delà) ; non déclaré → traverse + encart ⚠ « n piquages sans état DH ». **États DH d'extrémité** dans la fiche pièce (`elView` : endcap, endpoint, tés verticaux) : 3 boutons non / coiffe / sortie de coiffe, signé-daté, photo via la fiche — stockés `NET.dhData.ends` (clé line:elId). **Bouclage temporaire** : bouton ⟲ dans la fiche soudure (pose / retrait), `NET.dhData.temps[weldId]`, icône ⟲ dans le tableau + chips ✕ dans l'onglet. **Fermeture réelle de la boucle** (`D.close`) : bouclage temporaire le plus proche > bout de ligne déclaré bouclé (si tout raccordé : R = ρ·(totalE+totalN)) > « pontage supposé au dernier raccordé » (comme avant) — l'okbox et la mesure s'alignent dessus. **Mesure en un point enrichie** : boucle Ω (verdict à ± tol %, param déf. 5), isolement MΩ (< isoMin → « humidité probable »), localisation appareil (m de fil ou % de boucle → fil E/N déduit + `locate()`), **💾 Enregistrer la mesure** → `NET.dhData.mesures[]` (date, par, valeurs, verdict) + tableau « Dernières mesures ». **Défaut sur le plan justifié** (« pas juste un point ») : `renderDhOverlay` dans `<g id="dhG">` — trajet du fil surligné (pointillés animés, halo) depuis le départ en passant par les antennes (chaîne parentM), **cote « x m ► »** à mi-trajet, **zone d'incertitude** ± 2 % (mini 3 m) en bande rouge translucide, point pulsant, étiquette « défaut ≈ x m de fil · P6 · ± y m » ; activé par « Voir sur le plan » (mesure ou réflectomètre), effacé au changement de chantier. `NET.dhData` synchronisé comme hydro (saveDhData débouncé, kv handoff, conservé par le traceur : bridge prev.dhData + siteRef ×3). **Bug préexistant corrigé** : `bouclageSVG` crashait (« reading 'm0' ») quand le défaut localisé tombait dans une antenne d'un chantier traceur (cherchait `e.branch`, jamais posé — seulement `branchLine`) → find élargi + garde. Tests : `e2e_dh2.mjs` (11 checks : auto AXIOM=cuivré, purge coiffe → +4 m sur le NU, vidange non bouclée → coupure sur l'ÉTAMÉ (inversion té down validée), pont ⟲ ferme la boucle, mesure/verdict/iso/localisation, enregistrement + historique, défaut sur plan trajet+cote+zone, fiche bouchon 3 états) ; e2e_dh (ancien), hydro, home, hydro_test 43 verts. **RESTE (livraison B annoncée) : manchon en 4 sous-étapes** (soudure+visuel / fils+testeur+R attendue depuis dhLoop+close / manchon+pression / finition) — maquette validée écran 4.
- **20/08 (suite 8) — BUG « je n'ai plus mes chantiers » (accueil vide alors qu'Ethan est connecté ; « ça datait de juste avant le dernier push, depuis la maquette »)** : rien n'était perdu (serveur intact) — c'était un MASQUAGE local fautif et persistant. Chaîne du bug : `listSiteMeta()` échoue transitoirement (délai 15 s, ou verrou d'auth supabase-js quand traceur + appli sont ouverts, ou appels Supabase DANS le callback `onAuthStateChange` = deadlock connu qui rend `getSession()` muet 4 s) → `onAuth` passait `norm=[]` (au lieu de null) à `pushHandoffs` → la branche « chantier déjà envoyé absent d'une liste serveur valide = supprimé ailleurs » masquait TOUS les handoffs `sent` dans `trace:hiddenAt` (localStorage) → ensuite tout les filtre (boot, accueil, métas serveur) : écran « aucun chantier » définitif sur cet appareil même quand le serveur répond. Correctifs : (1) `sync.onAuth` sort le callback du dispatch (`setTimeout 0`) — plus de deadlock d'auth ; (2) `session()` distingue « verrou/blocage » (sentinelle LOCKED) de « pas de session » et retombe sur la DERNIÈRE session connue (poussée par les événements d'auth, fraîcheur vérifiée par expires_at) ; (3) `listSiteMeta` : la sélection enrichie (bbox/nbox/nw pour la carte d'accueil) se REPLIE automatiquement sur la sélection simple si le serveur la refuse (400) ; (4) `onAuth` : liste inconnue = null JAMAIS [] vers `pushHandoffs` (try/catch), `norm` porte désormais bbox/nbox/nw (pins + % des chantiers non ouverts localement), blocs tombstones/options sous try/catch → `renderHome` tourne quoi qu'il arrive ; (5) `pushHandoffs` : ne masque que sur une liste non-null ET NON VIDE (une suppression laisse toujours une pierre tombale dans la liste — une liste vide n'est jamais une preuve) ; (6) **RÉPARATION AUTOMATIQUE** : à chaque liste serveur valide, un chantier présent dans `trace:hiddenAt` mais PAS dans `trace:hiddenSites` (= masqué automatiquement, pas par un « Supprimer » manuel) et vivant sur le serveur (non deleted/builtin) est DÉ-MASQUÉ → les chantiers d'Ethan reviennent seuls à la prochaine connexion, sur tous ses appareils touchés. Test `e2e_home_safe.mjs` (Supabase entièrement mocké par page.route + session fake `sb-pghftlepduvfazbiavhq-auth-token` dans localStorage — piège : un pattern glob `**domaine/**` ne matche PAS dans Playwright, utiliser un prédicat url) : A panne 500 → rien masqué, copies locales à l'écran ; B hiddenAt {S1 auto, S2 manuel} + serveur vivant → S1 revient, S2 reste caché ; C sélection enrichie 400 → repli, liste peuplée. Régressions e2e_home, e2e_hydro, e2e_dh2, hydro_test 43 : verts.
- **20/08 (suite 9) — DH livraison B : mesure en DEUX boucles réelles + manchon en 4 sous-étapes (retour d'Ethan : ponts ⟲ à S-0369 et S-0377, mesure à S-0372 → « je devrais voir deux longueurs, deux valeurs, amont et aval — et là j'ai rien ; je ne retrouve pas les interfaces de la maquette »)** : l'ancienne « mesure en un point » raisonnait « boucle depuis le départ » (amont = simple cumul depuis PK 0, aval = fermeture globale D.close qui exigeait la continuité depuis le départ) → avec le début du réseau non raccordé, rien ne sortait. Réécrit en **continuité électrique réelle depuis le point** : `dhAtPoint(D,row)` parcourt chaque direction indépendamment jusqu'à une FERMETURE (pont ⟲ — même sur un manchon non raccordé, le pont relie les fils là — ou bout de ligne déclaré bouclé) ou une OUVERTURE (premier manchon non raccordé, coupure de fil `cutsE/cutsN` exposées par dhLoop : piquage « fils en attente », conn X, départ, bout non bouclé) → **deux boucles À PART avec chacune ses m étamé/nu et son Ω attendu** ; verdict : la mesure saisie est rapprochée de la boucle la plus proche (« ✓ Correspond à la boucle amont (fermée au pont ⟲ S-0369) ») avec l'autre valeur rappelée en cas d'écart ; localisation réflectomètre sur la boucle active (direction choisie par la mesure, sinon l'unique fermée, sinon l'aval) avec les deux sens de parcours du nu ; pont posé AU point de mesure signalé (« mesure ≈ 0 ici »). Fermeture sur une antenne série (dN inconnu) : détectée mais « calcul non géré » affiché. **Manchon en 4 sous-étapes (maquette écran 4 validée)** dans la fiche soudure, section propre : ① Soudure (photos + contrôle visuel) ② Fils raccordés (photos fils + testeur, **« Attendu au testeur ici » calculé par dhAtPoint** — les boucles fermées du moment, champ mesuré Ω, masse/continuité) ③ Manchon posé (rétracté/électrosoudé, photos + mano, test de pression) ④ Finition (bouchons, photo finale) ; chaque étape validée = qui/quand (compte connecté), photos uploadées au bucket (dataURL en secours hors ligne), annulable (« erreur de saisie ») ; `j.steps` persisté (sync.saveWeld data.steps, applyRemoteWelds, wipeWeld) ; **les grands statuts du plan ne changent pas** ; sur le plan, **anneau vert ¼/½/¾/plein autour de la pastille** (vues détail et moyenne) — piège : `pathLength` posé via innerHTML est minusculisé et IGNORÉ par SVG → dasharray en circonférence réelle + `data-ring`. UI `<details>` .dstep (CSS index.html), étape courante ouverte/orange. Tests `e2e_dh3.mjs` (scénario exact d'Ethan : 2 ponts autour d'un tronçon dont le début du réseau N'EST PAS raccordé → deux boucles fermées 0,30/0,90 Ω, verdict amont puis aval reconnus, pont retiré → direction ouverte, fiche 4 étapes + 2 attendus, étapes 1-2 validées → anneau 2/4 ; pièges tests : la vue est {tx,ty,k} pas {x,y}, zoomer à k≥12 sinon aucune pastille, les markers ne sont pas sous #map) ; régressions e2e_dh2 (11 ✓), e2e_dh (verdict devenu « aucune boucle fermée » — normal), hydro_test 43, e2e_home : verts.
- **20/08 (suite 10) — retours d'Ethan sur la mesure DH (« visuellement pas fou ; fil/piquage je ne sais pas ce que ça veut dire ; on ne sait pas si la mesure saisie est amont ou aval ; le réflectomètre part du point 0 au lieu du manchon choisi ; un poil plus d'explication sur le calcul ohmique »)** : (1) le tableau amont/aval devient **deux CARTES cliquables** (.dhdir : direction, gros Ω attendu, fermeture, détail étamé+nu=total ; carte grisée « ouverte » avec la raison) — **la carte cliquée = la boucle qu'on mesure** (state.dh.dir, présélection = première fermée, pastille « ● celle que je mesure », le label du champ le rappelle) ; le verdict compare à LA direction choisie et, si la valeur colle mieux à l'autre boucle, l'écrit (« ⚠ ta valeur colle avec la boucle aval — vérifie de quel côté tu as mesuré ») ; la localisation (m ou %) travaille sur la direction choisie ; (2) **« Le calcul, pas à pas » dépliable** : m étamé + m nu = m de fil × Ω/km ÷ 1000 = Ω, avec les vrais chiffres de la boucle choisie + rappel piquages et rôle du pont — NB : les 0,02/0,11 Ω de son screenshot = son réglage « fil » à ~1,2 Ω/km (pas un bug), le détail le rend visible ; (3) paramètres renommés avec infobulles : « fil (Ω/km) », « fil monté dans un piquage … m (aller) » ; (4) **réflectomètre : « Branché à » = le manchon choisi (défaut) ou le départ + « Je vise » amont/aval** — dFil = dE|dN du manchon ± distance (state.locate.from/dir ; erreurs propres : fil nu absent sur une antenne, distance > fil vers l'amont) ; résultat avec la mention « x m depuis S-… vers l'aval sur l'étamé ». e2e_dh3 adapté (cartes, clic carte aval avant le verdict aval, carte off au pont retiré, check 3b réflectomètre depuis manchon : 5 m depuis S-0003 → PK 29 ✓) ; e2e_dh2, e2e_dh, e2e_home verts.
- **20/08 (suite 11) — « distance annoncée pas visuel » + « garder la mesure avec l'historique du bouclage à l'instant t »** : (1) réflectomètre : champ « Distance (m) » élargi (96 px, plus écrasé entre label 2 lignes et bouton), selects « Branché à / Je vise / Fil » en flex:none avec min-width et libellés courts (« S-0372 · choisi », « X1 · départ ») ; (2) **chaque mesure enregistrée (💾) embarque le bouclage de l'instant t** — la valeur n'a de sens que par rapport à lui : direction (amont/aval), fermeture (« pont ⟲ S-xxxx » / bout bouclé), longueurs étamé/nu, valeur attendue + tolérance, verdict boucle (okLoop) EN PLUS du verdict isolement, réglage Ω/km, et la liste des ponts ⟲ posés à ce moment-là (mesCtx construit au rendu, poussé dans dhData.mesures) ; l'historique « Dernières mesures » a une colonne « Bouclage à l'instant t » (direction · fermeture · longueurs, ponts du moment en infobulle) et le ✓ combine boucle + isolement ; anciennes mesures sans contexte : « — » (compatibles). Pas de photos ici : Ethan confirme que les photos vivent dans les étapes manchon. e2e_dh3 check 2c (mesure sauvée : dir aval, pont ⟲ S-0006, attendu 0,9, okLoop, 2 ponts, colonne visible) ; e2e_dh2 vert.
- **20/08 (suite 12) — « ça ne part pas du manchon choisi » (le trajet sur le plan) + « enlève le mot réflectomètre »** : le POINT localisé était juste (PK correct) mais l'overlay du plan dessinait toujours le trajet depuis le départ de la ligne et l'étiquette disait « défaut ≈ 0 m » (state.loc du chemin réflectomètre ne portait ni dFil ni le manchon de branchement). Corrigé : `state.loc` porte `dFil` (distance saisie) + `from:{line,pk,weldId}` (les deux chemins : carte Localiser ET localisation de la mesure en un point — from y était un simple string, harmonisé en objet) ; `renderDhOverlay` TRONQUE le trajet au manchon de branchement (segsP[0].m0=from.pk, segments amont supprimés ; marche aussi défaut en amont du manchon ou dans une antenne), dessine un **badge bleu « M » + « MESURE : S-xxxx »** au branchement (comme la maquette validée), et la cote à mi-trajet affiche la **distance de fil depuis le branchement** (« 5 m ► », plus le PK). Titre de la carte : « Localiser un défaut » (sans « réflectomètre », idem le texte d'aide → « L'appareil annonce… »). e2e_dh3 3c (clic Voir sur le plan → badge M S-0003, cote 5 m, étiquette défaut ≈ 5 m, trajet présent) ; e2e_dh2 « défaut sur plan » et e2e_dh verts.
- **20/08 (suite 13) — ONGLET STOCK (stockage pré-isolé, maquette validée « 5 écrans » ; retours intégrés : reconnaissance BL confirmée ligne à ligne OK, zone d'emprise juste « que le gars n'ait pas de doute », « depuis le calepinage » gardé pour la fourniture propre, compte-rendu en 1 clic, un camion peut avoir PLUSIEURS zones, vue réseau gris/surbrillance selon le stock)** : nouvel onglet entre Hydro et Liste. **Modèle `NET.stock`** {zones (x,y,w,h,rot en m monde, status prevu/ok, photos, liv), lots (zone,key=stockKey,label,kind,dn/dn2/gaine/len/angle,qty), livs (label,bl,prevu[],ecarts[],by/at), moves (traçés datés-signés), takes (weldId,zone,key=**matchKey**,qty)} — reste = lots − takes ; `zoneAgg` impute les takes par clé de RAPPROCHEMENT (matchKey sans gaine : le réseau raisonne DN, certains BL manchons en gaine — piège corrigé : stockKey≠matchKey donnait pris=0). **`src/stock.js`** (module pur) : `parseBL(texte)` reconnaît les BL RÉELS d'Ethan — AXIOM (codes `[R-200/315-12]`/K/TW/NT/NK/P/E + « n Unités »), Renalia (refs `C90-60/14`, `TS-139/25`, DHEC + Ø/gaine → DN par `dnOfOd`, Qté livrée), LOGSTOR/Kingspan (sections Tube/Coude/SX-WPJoint, « 168 / 250 mm 12 m », n° article 10-14 chiffres, palettes ignorées) ; packing list ZPU = SCAN sans texte → refus propre (« saisis par le catalogue ») ; fixtures = les 5 vrais PDF dans `app/test/bl/` (**hors Git : .gitignore — données clients, dépôt public !**). **Livraison** : modal 3 sources (catalogue kind+DN+qté ; BL PDF via pdf.js déjà intégré, texte reconstruit par lignes Y, chaque qté confirmée ; « depuis le calepinage » = besoin `stockNeeds` − déjà livré) → **pose de la zone sur LE PLAN au tap** (pattern hydroPose : #stockBar, 13 × 3 m, glisser = déplacer, poignées W/H, poignée ↻ + bouton ↻ 15°, Échap/Terminer), hachurée jaune = prévue, pleine verte = déchargée, étiquette nom + pcs restantes. **Déchargement** : photo obligatoire (bucket, dataURL en secours), pointage prévu↔réel par ligne, écarts → liv.ecarts + le stock démarre au réel. **Scission / transfert / multi-zones** : cocher les restes (manchons/kits/DHEC pré-cochés), qté partielle, destination = zone existante ou NOUVELLE à poser (→ base vie ; un camion se répartit ainsi sur n zones), lots splittés + `moves` datés-signés. **Compte-rendu de réception en 1 clic** (popup imprimable : pointage, écarts « réserve chauffeur », photos, signature). **Exports CSV** zone + général (BOM, « ; »). **Prélèvement à la soudure** (maquette écran 5) : dans « Déclarer soudée », chips « Pièce prise dans quel stockage ? » (stockZonesFor : zones ok qui ONT la pièce — matchKey + manchons rapprochés par gaine≈casing — triées par distance, la plus proche pré-choisie, « hors stock » possible) → take au save ; pareil « Déclarer manchonnée » pour le manchon (manchette nue : un seul manchon pour les 2 soudures — le 2e ne redécompte pas) ; wipeWeld/reset → takes retirés. **Vue « le réseau et la matière »** : SVG du réseau — gris = posé (une soudure adjacente ≥ soudée), **vert = posable avec le stock restant** (déroulé pièce par pièce, lignes les plus avancées d'abord, stock global décrémenté virtuellement), orange pointillé = au-delà du stock, avec les ml posables. Stock général + alerte « il en faut n au calepinage ». saveStock débouncé (SITES+kv+serveur), conservé par le traceur (bridge prev.stock + siteRef ×3), reset setupSite, rôles chef/bureau (le prélèvement reste ouvert au soudeur/manchonneur dans son formulaire). Tests : `stock_test.mjs` (39 ✓ : dnOfOd, les 4 BL réels ligne à ligne, agrégats) ; `e2e_stock.mjs` (11 ✓ : livraison catalogue → pose → cartes+vue matière → chips → take → 12→11 → scission Base vie → **vrai BL AXIOM lu en PDF dans le navigateur (11 lignes)** → zone prévue hachurée → déchargement photo+écart → CR/CSV) — pièges : pdf.js du CDN bloqué dans le conteneur → `?pdfjs=/pdfjs/` (param existant) + copie locale de pdfjs-dist via npm, injecté par history.replaceState sans recharger ; régressions dh3/dh2/hydro/home/bridge/hydro_test 43 : vertes.
- **20/08 (suite 14) — « ça part du tube d'à côté » (défaut DH sur plan)** : l'overlay (trajet, badge M, point du défaut, zone, cote) était dessiné sur `posAtChainage`/`subAxis` = l'AXE de la ligne — qui pour une ligne traceur est `line.els = cond.A` : mesurer la conduite RETOUR posait tout sur le tube ALLER (l'indice sur son screenshot : l'anneau de la pastille S-0372 était BLEU = retour). Ajout de `condPos(l,c,m)` (interpolation sur l'axis de l'el de LA conduite au chaînage) et `condSubAxis(l,c,a,b)` (pièces entières + bords échantillonnés au pas de 1,5 m) ; `renderDhOverlay` dessine tout avec `r.cond`, et le badge M est posé par `jointPos(l, from.idx, cond)` = exactement le manchon (from porte désormais idx). e2e_dh3 3c vérifie la géométrie (distance badge ↔ els[idx].to = 0) ; e2e_dh2 défaut-sur-plan et e2e_stock verts.
- **20/08 (suite 15) — STOCK v2 (7 retours d'Ethan + demande traçabilité envoyée dans la foulée)** : (1) **camions ATTENDUS** : les lots d'une livraison « prévue » portent `pend:true` — comptés « attendus », pas en stock ; `zoneAgg` sépare qty/pend ; statut de zone DÉRIVÉ (`zoneStatusOf` : du validé → verte, que du pend → hachurée) ; **pointage PAR LIVRAISON** (openStockUnload(livId) : photo + réel, valide d'un coup toutes les zones du camion) ; (2) **PRÉ-répartition avant l'arrivée** (openStockPreSplit : répartir les lots pend sur plusieurs zones — « ça ne veut pas dire que le camion est arrivé », le pointage reste dû ; moves marqués `pre`) ; (3) **zones réutilisables** : la nouvelle livraison choisit « nouvelle zone à poser » OU une zone existante (un 2e camion dans la même zone) ; (4) **TRAÇABILITÉ CAMION** : registre « Livraisons / BL » (nom, BL — même fictif —, date, statut, zones, reste issu du camion, pointage/répartir/CR par ligne) ; chaque lot porte `liv` et le GARDE aux transferts (doMove splitte par lot et fusionne par (zone,clé,liv)) ; chaque prélèvement est imputé au plus ancien camion encore en stock (FIFO, `take.liv`) ; `livBreakdown(zone)` = « d'où viennent les pièces » affiché dans la carte zone ET la fiche ; (5) **fiche zone au TAP sur le plan** (openStockZoneModal : reste/attendu/provenance/boutons — 1er tap fiche, re-tap édition poignées) ; (6) **confirmation du prélèvement** : « hors stock » alors qu'une zone l'a, ou zone ≠ la plus proche → confirm ; refus → le save s'arrête (stockDoPick renvoie false) ; (7) **mousse PU** : kinds puKit/puA/puB (catalogue sans DN) — au décompte d'un manchon, 1 pochette sinon 1 produit A + 1 produit B pris automatiquement dans la même zone (toast) ; (8) **vue matière par tronçon** : select « Tout le réseau / <ligne> + antennes » (state.stockMatSel), projection et zoom sur la sélection ; (9) **onglet compact 30 camions** : zones et registre en `<details>` repliés (ouverts si ≤2 zones / ≤4 livs), compteurs en tête ; (10) **diag BL** : rien de reconnu → repli « Ce que je lis dans ce PDF » (8 lignes) pour m'envoyer le format — et les BL LOGSTOR + Renalia RÉELS sont maintenant testés via pdf.js NAVIGATEUR (9 et 4 lignes) en plus d'AXIOM (11). Robustesse : `canvas.setPointerCapture` sous try (pointeurs synthétiques). e2e_stock 15 checks verts (dont pré-répartition 2 zones/146 pcs pend, pointage multi-zones avec écart, tap fiche par PointerEvent dispatché — piège : mouse.click à coordonnées calculées atterrit sur BODY selon le layout) ; stock_test 39, dh3, home, hydro verts.
- **25/08 — STOCK v3 (session de reprise, 7 retours + dates)** : (1) **suppression d'une livraison** (✕ dans le registre — le « Camion 1 » de test qu'Ethan n'arrivait pas à virer : il n'y avait QUE la suppression de zone, et elle était planquée dans un `<details>` replié) : retire la liv + ses lots, dé-rattache ses prélèvements (`take.liv=null`, jamais d'orphelin), et supprime les zones que CETTE livraison laisse vides ; (2) **passage prévu → reçu rendu évident** : bandeau orange en tête d'onglet « N camion(s) annoncé(s) » avec un bouton par camion « 📦 <nom> (date) est arrivé — pointer », et le bouton du registre renommé « 📦 Camion arrivé » (avant : « Pointage », invisible) ; (3) **DATES de livraison** : `liv.date` saisie à la création (défaut aujourd'hui), affichée au registre avec le relatif (« demain », « il y a 2 j (en retard) »), **décalage par 📅** (`openStockDate`) qui garde TOUT l'historique des reports (`liv.dateHist` : de → vers, qui, quand, motif) — badge « décalé ×N » avec le détail en infobulle, historique repris dans le compte-rendu de réception ; (4) **la vue matière SVG est retirée** (retour d'Ethan : « avoir tout le réseau avant d'être bien avancé n'aide pas ») → remplacée par le **récap Besoin / Posé / Reste à poser / Stock / Attendu / Solde** (`stockBalanceHTML`, `stockPosed`, `stockNeedsFor`) : bandeau rouge « Il manque … », bandeau jaune « Surplus prévisible … », lignes colorées, filtre par tronçon (le select sert enfin à quelque chose) + CSV dédié ; (5) **mousse regroupée** : puA/puB/puKit fusionnent en **un seul produit « Mousse PU (A+B) »** (`isPU`, matchKey → 'pu'), 1 dose par manchon au besoin ; (6) **la mousse se choisit AU MOUSSAGE** : l'étape 4 du manchon devient « Moussage + bouchons de finition » et porte son propre sélecteur de zone (plus de décompte automatique avec le manchon — c'est le manchonneur qui dit d'où vient sa mousse) ; le tube reste choisi à la soudure, le manchon au manchonnage. Bugs corrigés au passage dans `stockPickHTML` : le test « déjà décompté » mélangeait les modes (une mousse prise faisait croire que la pièce l'était), et l'absence de stock renvoyait du vide au lieu de le dire. `closeSheet` exposé sur window.TRACE (les tests ne pouvaient plus changer d'onglet, la fiche couvrait la tabbar). e2e_stock **19 checks verts** (dont récap besoin/solde, mousse au moussage avec décompte, suppression de livraison sans orphelin) ; stock_test 39, dh3, hydro, home verts.
- **25/08 (suite) — DH v4 : l'étape « fils » devient une vraie étape DH (5 retours d'Ethan après son 1er manchon de test)** : (1) l'étape 2 du manchon demande **la boucle (Ω) ET l'isolement (MΩ)**, avec comparaison immédiate à l'attendu (tolérance) dès la saisie ; (2) **le schéma de câblage est revenu** là où il manquait : `wiringSVG` cliquable (fils tendus, heures de sortie, plongée) + les deux selects amont→aval DANS l'étape 2 — on relie les fils du doigt, l'inversion est détectée et écrite ; une fois l'étape validée le schéma reste affiché en lecture seule ; (3) **l'attendu au testeur est FIGÉ** à la validation (`steps[2].dh`) : direction, fermeture (pont ⟲ + son n°, ou bout bouclé), longueurs étamé/nu, attendu, mesuré, isolement, Ω/km, tolérance et **la liste des ponts posés ce jour-là** — l'encart garde son sens même quand les ponts ont bougé depuis (vérifié par test : pont retiré → l'encart tient) ; (4) **cliquer l'encart rejoue le bouclage d'alors sur le plan** (`showFrozenLoop` + mode `loop` de `renderDhOverlay`) : trajet surligné du manchon jusqu'à la fermeture, badge bleu « MESURE : S-xxxx » au départ, pastille violette ⟲ à la fermeture avec « attendu x Ω · mesuré y Ω », et la date de la boucle à mi-trajet ; la fermeture est retrouvée même si le pont a été retiré (recherche par n° de soudure) ; (5) **le transfert « erreur de saisie » est de nouveau proposé** (il était conditionné à `hasData` et disparaissait ; désormais toujours offert au chef/bureau, libellé explicite) et `TFIELDS` emporte `steps` — les 4 sous-étapes, leurs photos, leurs gars et le bouclage figé suivent la soudure transférée. Test `e2e_dh4.mjs` (6 checks : schéma+champs, clic fils→inversion, figeage complet, survie au retrait du pont, rejeu sur le plan, transfert) ; e2e_dh3, dh2, dh, stock 19, hydro verts.
- **25/08 (suite 2) — MODÈLE UNIQUE : fin des deux modèles concurrents (« deux modèles s'opposent : j'ai déclaré soudée avec photo, et l'étape 1 du manchon n'est pas cochée alors que c'est la même chose »)** : l'appli avait deux chemins parallèles pour le même acte — les gros statuts (formSoudee / formManchon, avec leurs blocages) et les 4 sous-étapes (avec les leurs). Désormais **tout se déclare dans les sous-étapes** et le reste en découle : (1) les boutons d'action ne sont plus des formulaires, ils **déplient l'étape suivante** (`data-gostep`, surlignage 1,2 s) selon le rôle — le soudeur ne voit que ① , le manchonneur ②③④, le chef tout (`canN(n)`) ; (2) **les blocages sont ceux d'avant, au bon endroit** : photo du cordon obligatoire à ①, photo + test de pression obligatoires à ③, et une étape ne se valide pas tant que la précédente ne l'est pas ; (3) **les statuts du plan dérivent** : valider ① → `status='soudee'` + événement `soudee` (procédé, coulée, photos de l'étape) ; valider ③ → `status='manchonnee'` + événement `manchonnee` (type de manchon, pression, inversion éventuelle) — le plan, le récap et les filtres continuent de marcher sans rien changer ; (4) **les prélèvements de stock suivent la même logique** : le tube à ① , le manchon à ③, la mousse à ④ (déjà fait la veille) ; (5) **migration silencieuse** (`migrateSteps`) : une soudure déclarée avant (ou par un autre appareil, ou par le serveur) coche ses sous-étapes à l'ouverture de la fiche, en reprenant l'auteur, la date, les photos et le procédé de l'événement — plus jamais « soudée » d'un côté et « étape 1 à faire » de l'autre ; (6) **procédés simplifiés** à `TIG` / `Cellulosique` (au lieu des 4 codes 141/111/135). formSoudee/formManchon ne sont plus atteignables (code laissé en place, sans bouton). Test `e2e_steps.mjs` (7 checks : procédés, blocage photo, statut dérivé à ①, blocage pression, statut dérivé à ③, migration d'une ancienne soudure, droits par rôle) ; e2e_dh4 et e2e_stock adaptés au modèle unique (leurs scénarios passaient par les anciens formulaires) et verts ; dh2, dh3, recap, stock_test 39, hydro_test 43 verts.
- **25/08 (suite 3) — STOCK v4 (4 retours pendant les tests d'Ethan, le reste validé « propre »)** : (1) **fiche détaillée par livraison** (`openStockLivDetail` + `livBreakdown2` FIFO par référence) : contenu du camion (BL/reçu/écarts), reports de dates, restant ZONE PAR ZONE et par référence — accessible en touchant le NOM du camion dans le registre ou une chip de provenance d'une zone (fini le « XX pcs » sec) ; (2) **déplacement significatif d'une zone tracé** : un glisser ≥ 5 m enregistre un mouvement « 📍 Zone X déplacée de N m » (qui/quand/de→vers) dans l'historique — gesture.x0/y0 mémorisés au pointerdown ; (3) **carte d'ensemble des stockages** (`initStockMap`, même moteur que la vue hydro : pan/zoom continu, ortho IGN grisée si géoréférencé via `tilesFor` + filtre `stkGrayV`, sinon fond DXF/uni) : réseau en traits fins + zones étiquetées (nom · reste), cadrée sur réseau ∪ zones (la base vie compte), TAP sur une zone → sa fiche — en tête d'onglet, repliable ; (4) **tout l'onglet se replie et s'en souvient** : chaque bloc (carte, registre, zones, stock général, récap besoin, mouvements) est un `<details>` dont l'état vit dans `state.stockUI` (listener toggle) — par défaut seule la carte est ouverte ; le récap besoin/livré/reste porte son résumé dans le summary (« ⚠ il manque N références » / « ✓ couvert ») et le select tronçon est passé dans le corps. **Fix UX débusqué par le test** : le bouton « ↩ réseau hors écran » s'affichait par-dessus la carte quand on regardait une zone ÉLOIGNÉE du réseau (la base vie) et bloquait tout geste → il ne s'affiche plus si une zone de stockage est visible à l'écran. e2e_stock **23 checks** (fiche livraison, tap carte→fiche, drag réel ≥5 m tracé — pièges : le details fermé cache les boutons à Playwright, l'overlay #offscreen mangeait la souris) ; steps, dh3, dh4, hydro, stock_test verts.
- **25/08 (suite 4) — 5 retours étapes/DH : rotations à l'étape fils, manchon à la soudure, boucle amont entière, et le VISUEL SUIT LE FIL** : (1) **les rotations de tubes sont revenues au moment des fils** : l'étape 2 porte les boutons ⟲90/⟲15/15⟳/90⟳/⇄ des deux bouts (amont/aval), factoriés en `rotCtlHTML` (verrou inchangé : bout déjà manchonné = position figée) — ils avaient disparu avec l'ancien formulaire manchon ; (2) **le manchon se décompte à la SOUDURE** (étape 1, libellé « Manchon enfilé sur le tube — pris dans quel stockage ? ») : il s'enfile AVANT de souder, le choisir à l'étape 3 n'avait pas de sens terrain — le pick et le `stockDoPick('sleeve')` sont passés de l'étape 3 à l'étape 1 (tube + manchon au même moment, mousse toujours au moussage) ; (3) **n° de coulée supprimé** (« on s'en fiche ») : champ, collectStep, événement, ancien formulaire ; (4) **l'attendu s'affiche même SANS fermeture déclarée** : fils continus du départ jusqu'au manchon → l'étape 2 affiche « Attendu au testeur ici : x Ω (amont — boucle amont entière, départ → ce manchon) » avec l'explication (vaut si le départ est bouclé à la centrale, ou en pontant ⟲ ici) = `dj.row.R` de dhLoop quand `!row.open` ; FIGÉ pareil (`closureId:'self'`) et rejouable — `showFrozenLoop` trace alors depuis le DÉPART avec la pastille ⟲ sur le manchon même ; (5) **le visuel de localisation SUIT LE FIL** (sa photo : le point était au milieu du tube) : `wirePath` annote chaque segment du fil PHYSIQUE parcouru (`w`, mis à jour aux inversions via `j.conn`, entrée d'antenne par fil via `antennaMode(child,cond,e)`) ; nouveaux `offsetPl` + `wireTraceSVG` : le trajet est décalé de ±3 px du côté du fil physique (étamé un côté, nu l'autre, aller/retour d'antenne de part et d'autre), **couleur du fil** (étamé gris `#dfe4ea`, nu cuivré `#e2843a`), et à une inversion le trait CHANGE de côté et de couleur au manchon même ; le point du défaut est posé SUR le fil (offset du même côté) ; `state.loc.legs` = intervalles de fil parcourus, calculés aux 3 endroits (réflectomètre : du branchement à la distance ; localisation par la mesure : étamé jusqu'à la fermeture PUIS nu jusqu'au défaut — le vrai chemin du courant ; rejeu figé : les DEUX fils entre manchon et fermeture, `filAt` retrouve les distances de fil des deux soudures sur chaque walk) ; repli sur l'ancien tracé d'axe si les legs manquent. Confirmé au passage : la config du fil dans les tés existe déjà (fiche sortie de té : série/bouclée/non raccordée + quel fil) et `locate` suivait DÉJÀ les inversions — c'était le dessin qui ne les montrait pas. Test `e2e_dh5.mjs` (9 checks : rotations opérantes depuis l'étape 2, plus de coulée, attendu 0,30 Ω « boucle amont entière » affiché ET figé `self` puis rejoué (2 fils tracés), inversion → `seg.w==='N'` et tracé bicolore, pick manchon à l'étape 1 avec décompte au stepok(1)) ; **e2e_dh3 adapté** (le blocage photo du modèle unique l'avait cassé : photo seedée avant l'étape 1, comme dh4) ; dh2, dh3, dh4, steps, stock 23, home_safe verts.
- **25/08 (suite 5) — STOCK v5 (5 retours pendant qu'Ethan teste les fils)** : (1) **la mousse PU est PAR DN** (`matchKey` → `pu:<dn>` ; `pu:` = anciens ajouts sans DN, qui dépannent n'importe quel DN en repli — proposés au moussage, ligne à part dans le récap) : le besoin compte 1 dose au DN du manchon, l'étape 4 demande « Mousse PU (A+B) DN100 », le take porte `pu:100` ; (2) **au catalogue, ajouter un manchon ajoute automatiquement sa mousse du même DN** (même qté, toast, ligne supprimable) — et la mousse demande un DN désormais ; (3) **l'enveloppe extérieure affichée** : le DN saisi est le DN acier, le manchon se pense en Ø gaine → libellés « Manchon DN100 · Ø200 » partout (catalogue via `gaineMM` = `casingOf` fournisseur/série, besoin, pick de l'étape 1, BL AXIOM qui portent déjà la gaine), et un hint « Ø ext. gaine ≈ N mm » sous le select DN ; (4) **BUG dates corrigé** : le modal Nouvelle livraison se re-rend à chaque ajout de ligne et ÉCRASAIT la date saisie (et le nom, le BL, la case prévu, la zone) → `keep()` relit tous les champs avant chaque re-render — la date du 28/08 tient maintenant jusqu'au Valider ; (5) **fiche zone = provenance PIÈCE PAR PIÈCE** (retour : « j'ai juste la zone d'où ça vient et le nombre au global ») : nouveau bloc « Provenance — pièce par pièce » : chaque lot avec son reste, son CAMION d'origine (chip 🚚 cliquable → fiche livraison) et son **parcours complet daté** (« Zone 1 (déchargé 28/08) → ici le 25/08 », ou « déchargé directement ici ») ; `zoneLotsDetail` (reste par LOT, FIFO comme livBreakdown) + `lotTrail` (remonte les transferts à rebours par clé de rapprochement + camion, anciens moves rapprochés par libellé, 12 étapes max) ; les `moves` portent désormais `key` (matchKey, pas stockKey — le 1er essai ne matchait jamais) et `livs` (camions touchés) ; `openStockZoneModal` exposé sur TRACE pour les tests. Test `e2e_stock2.mjs` (10 checks : Ø gaine, mousse auto + date conservée à travers 2 re-renders, lots aux bonnes clés, moves clé+camion, provenance détaillée avec parcours daté, chip → fiche livraison, « déchargé directement ici », récap DN + générique à part, étape 4 par DN avec repli, take pu:100) ; e2e_stock adapté (libellé mousse DN, clés `/^pu(:|$)/`) → 23 verts ; stock_test 39, dh3, dh4, dh5, steps, home_safe verts.
- **25/08 (suite 6) — tracé du fil v2 (retour : « pas très clair — décale plus, ou juste le fil en surbrillance rouge jusqu'au défaut ; et qu'on voie ce qui va dans l'antenne au té »)** : fini les deux couleurs par fil physique — **une seule surbrillance** : ROUGE (`#e8102d`) pour le trajet du branchement au défaut, VIOLET (`#8a2be2`, assorti à la pastille ⟲) pour le rejeu d'un bouclage figé ; décalage porté de 3,2 à **6 px** du côté du fil physique (la bascule aux inversions reste visible), trait 3,8 px sur halo blanc 9 px, pointillés animés ; `wireTraceSVG` réécrit : groupes **orientés dans le sens du parcours** (l'amont remonte, plus d'ordre m croissant aveugle — offset inversé quand la polyligne est retournée pour rester du même côté monde) et **toutes les jonctions reliées** par des connecteurs (`data-wtc`) : manchon d'inversion (le saut de côté devient un trait), entrée/sortie d'antenne au té, bout d'antenne (le U), et le passage étamé→nu au pont de fermeture — le chemin se lit d'un seul bloc, zéro ambiguïté ; au té, **on voit le fil plonger dans l'antenne** : aller d'un côté de la branche, retour de l'autre, U fermé au bout ; en mode boucle (2 fils parallèles) : pas de connecteurs bout à bout entre fils, mais les 2 fils reliés aux 2 extrémités (le testeur d'un côté, la fermeture ⟲ de l'autre) ; paths marqués `data-wtl="<ligne>"` (testable) ; point du défaut décalé pareil (6 px). Test `e2e_dh6.mjs` (4 checks : antenne série AXIOM sur le nu, localisation après l'antenne (170 m de fil), plongée visible — 2 paths sur l'antenne + 2 sur la parente + ≥3 jonctions —, défaut DANS l'antenne en branchOut) ; e2e_dh5 adapté (B3 violet + 2 fils `data-wtl`, C2 rouge sans les anciennes couleurs + jonction d'inversion) ; dh3, dh4 verts ; capture visuelle contrôlée.
- **25/08 (suite 7) — rotations qui sautaient + fils du té dessinés sur le plan (2 retours)** : (1) **BUG de fond : les rotations/retournements de tubes n'étaient persistés NULLE PART** (`el.rot`/`el.flip` vivaient en mémoire ; `finalizeLine`/`setupTraceurLine` remettent rot:0 à chaque reconstruction) → toute resynchro (`refreshSiteFromServer` après un save d'un autre module ou d'un autre appareil) ou rechargement les remettait droits — c'est le « ils se remettent en position droite après d'autres manipulations » d'Ethan. Fix : **`NET.elPos`** (`{'<ligne>|<cond>|<élément>': {rot,flip}}`), même pattern que dhData/stock : `recordElPos` appelé par les 5 gestes (dial `data-rot`, `data-rota/rotb`, `data-flipa/flipb`, `flip`, `rot0`), `saveElPos` débouncé (SITES + kv handoff + saveSite), **`applyElPos()` dans setupSite** après construction — et le bridge traceur conserve `prev.elPos` (une nouvelle version du tracé ne remet pas les tubes droits). Testé par rechargement complet de page : la rotation tient. (2) **au té, le plan montre enfin QUEL fil part dans la branche** : dans le rendu des fils (`renderPlan`, zoom ≥ 15 px/m), pour un él `tee` à branche dessinée, le fil concerné (réglage « sortie de té » de l'antenne s'il existe, sinon défaut fournisseur `wireOfTee` — LOGSTOR = étamé, AXIOM/Renalia = cuivré, inversé si té retourné) est **coupé au droit du piquage et dessiné plongeant dans la branche** (aller + retour légèrement écartés le long de la branche, paths `data-wtee`), l'autre fil traverse tout droit — avant, les deux traversaient. e2e_dh6 étendu à 7 checks (plongée `data-wtee` en cuivré ×4 au zoom 20, rot persistée avant/après reload) ; dh3, dh4, dh5, steps, stock2, home_safe verts ; captures contrôlées.
- **25/08 (suite 8) — annulation d'étape RÉELLE + le fil dessiné devient rouge (2 retours du soir)** : (1) **« quand j'annule une étape elle ne s'annule pas vraiment »** : l'undo supprimait `steps[n]` mais laissait le STATUT dérivé en place → à la réouverture, `migrateSteps` recochait l'étape depuis le statut (et l'événement). Fix : annuler fait tout RETOMBER — ① → statut `a_souder`, événement `soudee` retiré, tube ET manchon rendus au stock (takes filtrés) ; ② → fils dé-déclarés (`wire='a_raccorder'`, conn droit, cont/iso remis, le bouclage figé part avec l'étape) ; ③ → statut `soudee`, événement `manchonnee` retiré ; ④ → la mousse retourne au stock ; ordre imposé (on annule en remontant : la 3 avant la 1), `annule_etapeN` journalisé serveur. e2e_steps 8a/8b/8c (refus d'ordre, statut retombé + event retiré, fiche rouverte SANS re-cochage). (2) **tracé DH v3 — « pas un trait rouge en plus : le fil dessiné qui devient rouge »** (sa photo : le pointillé flottait entre les fils, illisible avec un tube tourné) : renderPlan mémorise la position dessinée de chaque fil (`e._wo` = offset conduite + heure de sortie, ROTATION comprise, par zoom) et `wireTraceSVG` v3 repasse dessus : surbrillance pleine (liseré sombre + rouge, plus de pointillé décalé), par élément via `axisSub`+`offsetPoly` (les mêmes que le rendu des fils → superposition au pixel) ; dans l'antenne l'aller emprunte un fil et le retour L'AUTRE (les deux traits deviennent rouges) ; le point du défaut est posé sur le fil dessiné de son élément ; dézoomé (fils non dessinés, <15 px/m) : repli sur l'axe du tube et les jonctions fusionnent naturellement — zoomé, les connecteurs (inversion, té→antenne, pont) apparaissent (cap 6 m). Rejeu figé : pareil en violet. dh5/dh6 adaptés (zoom avant de compter les jonctions ; bout d'antenne hors vue = pas de jonction à dessiner) ; dh3, dh4, steps 10, stock2, home verts. (3) **maquette « dessin des tés » envoyée à Ethan** (`app/maquette_tes.html`, non intégrée) : 3 variantes (A té plein congés / B té métier épaulement / C compact), toggles fournisseur (qui plonge) / té à saut / étiquettes, vue chantier (aller+retour+antenne, saut par-dessus le retour) + gros plan annoté — en attente de son choix ; noté : sur son chantier la conduite est quasi verticale et l'antenne part horizontalement → le dessin suivra l'angle réel de la branche, et le rangement des étiquettes autour du té fait partie du lot à coder.
- **25/08 (suite 9) — TÉS intégrés (maquette validée) + fil rouge fini + stock : carte de pick au doigt, Ø sur les mousses** : (1) **le té est dessiné en T PLEIN à congés** sur le plan (zoom détail ; dézoomé : trait simple) — path orienté à l'ANGLE RÉEL de la branche (`data-teep`), halo conservé pour les tés à saut ; (2) **fils au té selon la fiche « sortie de té »** : série = les brins traversent la soudure de sortie vers l'antenne ; **bouclée = pontés AU MANCHON de sortie, de part et d'autre** (précision Ethan : c'est le manchonneur qui ponte là, pas dans la pièce) — U côté té (`data-wteeu="tee"`, couleur du fil) ET U côté antenne (`"ant"`, cuivre neutre) ; non raccordée = brins en attente à mi-branche ; `linkTraceurBranches()` appelé en tête de renderPlan (sinon `branchLine` n'existait qu'après un calcul DH) ; (3) **LOGSTOR : positions étamé/nu INVERSÉES dans `WIRE_POS`** (E:25/N:335 — l'étamé prend la place du cuivré, aucun croisement, il fait « le chemin du orange ») — dial, schéma manchon, plan et surbrillance suivent tous `wirePos()` ; (4) **étiquettes** : taille plafonnée à 17 px écran (fini les pavés géants au zoom fort) et l'étiquette « sortie de té » rangée LE LONG de l'antenne ; (5) **fil rouge v4** : la surbrillance reprend le DEVANT/DERRIÈRE du fil (`_wo.fE/fN`) — pointillé du fil original (animé lentement) quand il passe derrière, plein + **filet lumineux animé** (l'ancienne animation, posée SUR le fil) devant ; violet pareil en rejeu ; (6) **stock v6** : mini-carte dans CHAQUE pick (étapes 1 et 4) — zones vertes = ont la pièce (touchables au doigt, `rect[data-stkpick]`), grises = ne l'ont pas, orange = choisie, point rouge « la soudure » ; sélection synchronisée carte ↔ pastilles (handler SVG-aware) ; **Ø gaine aussi sur les mousses** (« Mousse PU (A+B) DN100 · Ø200 ») : libellés, catalogue (directe et auto-ajoutée), besoin, pick. Tests : `e2e_tee.mjs` (5 : T plein, U tee+ant au manchon, none, étiquette plafonnée, LOGSTOR étamé plonge), `e2e_stock3.mjs` (4 : cartes dans les étapes, tap → bascule synchronisée → take de la zone touchée, mousse Ø au moussage) ; stock/stock2 adaptés (clé mousse avec gaine) ; les 10 suites vertes. Maquette `maquette_tes.html` mise à jour (U au manchon des deux côtés) = référence du rendu.
- **25/08 (nuit) — LE PAQUET DU SOIR, codé d'avance et DÉSACTIVÉ (demande Ethan : « code à fond, on ajoute demain »)** : tout vit dans **`src/next.js`** derrière des interrupteurs (`localStorage trace:next`), l'appli ne change pas d'un poil tant que rien n'est allumé ; l'activation se fait depuis le bouton **« ⏳ Nouveautés en attente » sur la HOME** (chef/bureau) — une case par nouveauté, rechargement à chaque bascule, pour vérifier UNE chose à la fois. Contenu : (1) **TS / hors marché** : au TRACEUR, fiche de la ligne, bloc « Marché / TS » (état : marché / TS proposé / TS commandé / forfaitaire + n° de TS) — `l.hm` transporté par le bridge ; sur le plan de l'appli : ligne HACHURÉE couleur d'état + badge « TS-01 · TS proposé » (`renderTsOverlay`, groupe #tsG) ; onglet Récap : bloc « Travaux supplémentaires » par TS (ml, soudures, état) avec bascule d'état chef/bureau persistée (state.lines + NET.lines + saveNetPart) — rebasculer « dans le marché » éteint les hachures, la trace reste ; (2) **Dossier administratif** (onglet « Dossier ») : 11 catégories (DT/DICT, plans exé, plans, qualifs, PGC, PPSPS, planning, habilitations, BL, accueil, autre), dépôt PDF/photo → **serveur** (`sync.uploadDoc`, bucket photos, URL signée 3 ans) et l'appli ne garde que la fiche ; hors connexion : secours dataURL si < 1,5 Mo sinon refus expliqué ; **croisement stock** : le BL PDF importé dans une livraison se classe tout seul en « Bons de livraison » (case cochée par défaut dans le modal livraison) ; (3) **QSE** (onglet) : Accueil chantier (questions **PROVISOIRES `ACCUEIL_Q` — à remplacer par celles d'Ethan dès réception**), rattache automatiquement le PPSPS du dossier (« l'émargement vaut aussi signature du PPSPS », alerte s'il manque) ; Quart d'heure sécurité (thème, points, questions `QUART_Q` provisoires) ; **n'importe quel PDF à faire émarger** (flash info…) ; émargement : nom (liste ou saisie) + **signature au doigt** (canvas, tablette du chef, les opérateurs passent chacun leur tour) ; **feuille d'émargement imprimable** par document (signatures incluses, lignes vierges en secours) ; (4) **tabbar allégée** : Catalogue et Liste cachés, accessibles par « ⋯ » ; (5) **Export DOE** (bloc dans Récap) : **carnet de soudage/manchonnage imprimable** — chaque soudure documentée : n°, ligne/conduite/PK/DN, statut, mini-vue du plan (réseau gris, ligne rouge, point), tableau qui-a-fait-quoi-quand (événements + 4 sous-étapes), DH figée, inversion signalée, photos (max 8). Hooks app.js : import next, `saveNetPart(key)` générique, dispatch `nextRenderTab`, home (bouton), récap (TS+DOE), plan (#tsG), stock (st2.blFile + case + `nextAdminAddBL`), `hm` transporté dans setupSite/setupTraceurLine ; bridge : `hm` + `prev.admin/qse` conservés ; traceur : bloc fiche ligne + badge liste (flag ts). Test **`e2e_next.mjs` (11 checks verts)** : flags OFF = rien ne change ; ON = onglets, hachures+badge, bascule TS persistée, dépôt PPSPS hors connexion, accueil + PPSPS rattaché, signature au doigt (souris réelle), feuille imprimable (popup), carnet DOE (popup, S-xxxx + photo + plan), panneau 5/5. Les 9 suites existantes vertes flags OFF. **À faire demain avec Ethan : activer un par un ; remplacer ACCUEIL_Q/QUART_Q par ses vraies questions ; brancher l'état TS sur le devis/chiffrage plus tard.**
- **25/08 (nuit, suite) — fils du té en RAILS (schéma Renalia d'Ethan) + about de la branche** : (1) ses captures montraient de grands **X blancs** au té : les brins de plongée étaient des DROITES de la position du fil vers le bout de branche — sur son cas réel (conduite quasi verticale, antenne latérale, aller/retour serrés) ça traversait tout. Réécrit selon SON schéma fournisseur : **raccord court perpendiculaire à la branche au droit du piquage, puis les deux brins montent PARALLÈLES le long des deux bords de la branche** (path 3 points M-L-L, `side1=sign((P1−b0)·n)` = chaque brin longe SON bord — zéro diagonale, zéro croisement) ; le U du mode bouclé se ferme entre les deux rails au manchon de sortie ; (2) **la règle du fil est confirmée telle quelle** : Renalia/AXIOM = CUIVRÉ dans l'antenne, LOGSTOR = étamé (`wireOfTee` inchangé) — s'il voit l'étamé plonger sur un chantier, c'est un RÉGLAGE : « fil dans la branche » forcé dans l'onglet DH (`state.dh.antWire`) ou le fil réglé dans la fiche « sortie de té » (`j.tee.wire`) ; (3) **il manquait l'about au bout de la branche** (sa capture) : la gaine du té s'arrête maintenant à `bague (0,09 m) + acier nu (0,15 m)` avant la soudure de sortie (`data-teeab`, polygones orientés dans le repère de la branche, acier `#aeb4bb` comme les tubes) — et comme sur les tubes, l'about disparaît quand la soudure de sortie est MANCHONNÉE (le manchon le couvre). e2e_tee check 1 étendu (rails 3 points + `data-teeab` ×4) ; capture contrôlée sur le cas incliné d'Ethan ; dh5, dh6, next, steps, stock3, home_safe verts.
- **26/08 (matin) — BOUCLAGE des fils ENTRE EUX au manchon + té : fil constant & raccord dans le calo + stock : carte lisible & suppression de zone propre (4 retours d'Ethan)** : (1) **« relier entre eux les fils du même tube »** : à l'étape 2 (et dans l'ancien formulaire), toucher les DEUX fils d'un même côté l'un après l'autre les ponte entre eux (`state.loopA` amont / `state.loopB` aval, re-tap = défait, « ∅ non raccordé » remet tout à plat) ; dessiné en **pont en U** entre les deux sorties de la tranche avec serti au milieu + badge violet « ⟲ BOUCLÉS ENTRE EUX » (`wiringSVG(...,loops)`, les 4 appels passent le bouclage) ; à la validation : `j.loopA`/`j.loopB` sur la soudure, `conn={X,X}`, `wire='raccorde'` (fini la fausse « inversion » quand rien ne traverse — l'inversion n'est plus détectée que sur un E↔N réel), transfert/remise à zéro/annulation/Supabase suivent (`TFIELDS`, `wipeWeld`, annulation d'étape 2 qui nettoie aussi l'état du formulaire, `sync.saveWeld` + application des welds distants aux DEUX endroits) ; **côté DH c'est une vraie fermeture** : `wirePath`/`dhLoop` coupent la traversée, `dhAtPoint` ferme la direction qui fait face au pont (`kind:'loopW'` → « fermée — fils bouclés entre eux au manchon S-xxxx », R calculée comme un pont ⟲) et depuis le manchon bouclé lui-même la boucle amont entière est fermée ICI (`kind:'loopSelf'`) ; figeage de l'étape 2 avec libellés et `closureId` (`self`/weldId) portés ; (2) **té — Ethan tranche : « dans TOUS les cas, même té retourné, c'est le même fil qui part dans l'antenne ; seul le côté où sortent les fils change »** → `wireOfTee` = constante fournisseur (plus AUCUNE inversion `vert==='down'`/`sautDir==='bas'`), et le libellé du réglage « sortie de té » affiche enfin le bon défaut (« nu (cuivré) » chez Renalia/AXIOM — il disait « étamé » à tort) ; (3) **« les fils dépassent du calo »** : le raccord des rails est clampé DANS le corps (`tU` borné à `[gap2·0,6 ; uL·0,55]` dans `mk6`) — tube tourné, le fil qui arrive du côté opposé traverse la mousse au lieu de sortir du té ; (4) **stock** : carte de prélèvement lisible dézoomée (étiquettes avec halo qui S'EMPILENT au lieu de se chevaucher + trait de rappel pointillé, pastille minimum quand la zone est minuscule à l'échelle, grande cible tactile invisible `data-stkhit` par zone — ignorée par le style de sélection) ; **supprimer une zone qui a encore des pièces ouvre une modale « où les basculer ? »** (`openStockZoneDelete`) : bascule TOUT (lots livrés ET prélèvements déménagent ensemble → les comptes restent justes ; mouvement tracé par référence → visible dans le parcours pièce par pièce), lots attendus re-pointés, « supprimer SANS basculer » reste possible mais explicite. Poignée de tests élargie (`TRACE.wirePath/dhLoop/dhAtPoint/dhDirLab`). Tests : **`e2e_loop.mjs` NOUVEAU (10 checks verts)** — 2 taps amont → pont + badge, validation figée « à CE manchon », coupure + « fermée au manchon S-0002 » (0,3 Ω), ré-ouverture, annulation réelle, 2 taps aval, modale bascule (reste 4), bascule Z1→Z2 (lot + take + move), carte sans chevauchement ; `e2e_tee` +2 checks (saut vers le bas = même fil ; rot 180° = raccords clampés) ; les 10 suites existantes re-passées TOUT VERT. Captures contrôlées (pont, carte empilée).
- **26/08 (matin, suite) — VERROU sur le fil de la branche (capture Saint Lô Zone Bleue d'Ethan : l'étamé plongeait ENCORE au té ; « c'est le rouge (le cuivré) qui doit aller dans l'antenne »)** : le code portait encore DEUX réglages capables de forcer le fil — « fil dans la branche » (étamé forcé) dans les paramètres de l'onglet DH (`state.dh.antWire`, localStorage par appareil) et le sélecteur « fil dans l'antenne » de la fiche sortie de té (`j.tee.wire`, stocké SUR LE SERVEUR donc appliqué sur tous les appareils — et son ancien libellé par défaut disait « étamé » à tort, piège parfait). **Tout est verrouillé** : `wireDefFournisseur()` = règle fournisseur PURE (Renalia/AXIOM = cuivré/nu, LOGSTOR = étamé), `antennaMode` IGNORE tout `j.tee.wire` déjà stocké (pas besoin de nettoyer le serveur), le changement de mode purge `j.tee.wire` au passage, les deux sélecteurs remplacés par un simple rappel non modifiable, `antWire` retiré de `DH_DEF`/`saveDH`. Au passage : un commentaire de fin de ligne inséré dans le handler `teeMode` avait avalé le reste de la ligne (node --check passait, rolldown non) — réparé ; leçon : **jamais de `//` en fin de rep quand la suite de la ligne d'origine continue derrière**. e2e_tee check 8 : Renalia avec `localStorage antWire:'E'` ET `j.tee.wire='E'` injectés → les rails restent CUIVRÉS et les sélecteurs n'existent plus ; capture de preuve `shot_renalia_te.mjs` (les 2 tés : cuivré plonge, étamé droit). Les 12 suites re-passées TOUT VERT. Si l'étamé plonge encore chez Ethan après ce build : c'est qu'il regarde l'ANCIEN build (pull + Ctrl+F5).
- **26/08 (matin, suite 2) — au té, les fils sont dessinés comme sur la PIÈCE : le fil qui plonge sort CÔTÉ ANTENNE (capture Ethan : « dans tous les tés, les fils qui vont dans l'antenne doivent être côté antenne »)** : jusqu'ici les fils du té gardaient la position horaire du tube (rotation comprise) — quand le cuivré tombait du côté opposé à la branche, son raccord traversait tout le tube (moche, faux par rapport à la pièce réelle). Maintenant, pour un té AVEC branche, les positions sont IMPOSÉES par la pièce : côté de la branche calculé contre la normale de l'axe dessiné (`sB`, convention `offsetPoly` = (ty,−tx), segment le plus proche de la naissance de branche), le fil plongeur reçoit l'offset `+sB·casing·EX·0,37`, l'autre `−sB`, les deux en trait plein (sorties à l'équateur de la pièce) — quels que soient rotation/retournement/sens de pose du tube ; `e._wo` suit → la surbrillance DH repeint au bon endroit. Le clamp du raccord (matin) reste en filet de sécurité. e2e_tee check 7 renforcé : tube tourné à 180°, les DÉPARTS des rails restent côté antenne (`mU>−0,02`) ET les raccords dans le corps ; capture de preuve refaite sur SON cas (conduite quasi verticale, antenne à gauche → cuivré à gauche qui plonge, étamé à droite qui file droit). Les 11 autres suites re-passées TOUT VERT.
- **26/08 (après-midi) — 6 retours d'Ethan d'un coup (fils ≤ jaune, abouts collés, té retourné, stock muet, défaut qui se propage, fiche sortie de té)** : (1) **fils arrêtés AU TRAIT JAUNE** : les rails de la branche du té s'arrêtaient à 12 cm du bout (sur bague+acier = 24 cm) → `tip=max(abL2+.02, …)` avec `abL2` calculé comme le dessin de l'about (0 si manchonnée) ; le U du mode bouclé se ferme au ras du jaune ; (2) **« acier-soudure-acier tout collé »** : abouts dès `Lax>0.36` (au lieu de 0.6 — les bouts 40-60 cm collaient) + cordon de soudure en 3 bandes (bourrelet sombre / corps / cœur clair, ±5 cm) au lieu d'un trait gris invisible ; (3) **« je ne vois pas l'option de retourner le té »** : fiche du té → « Pose du té : fils AU-DESSUS / ⤓ retourné — fils EN DESSOUS » (`e.teeDown`, persisté dans `NET.elPos` clé `td`, appliqué par `applyElPos`) → les fils du té passent en pointillé (derrière), la surbrillance DH suit (`_wo.f` false) — le FIL reste le même (règle fournisseur) ; au passage **bug tueur réparé** : `const a=b.dataset.act` → `a.startsWith` PLANTAIT tout le handler pour les boutons sans data-act (data-saut avait le même bug dormant) → `||''` ; (4) **« ça ne me demande plus dans quel stock »** : `stockPickHTML` rendait '' dès qu'aucune zone n'avait la pièce (DN différent du stock, zones vidées/supprimées après ses tests) → maintenant la question RESTE posée : warnbox « aucun stockage n'a cette pièce — dis quand même d'où elle vient (le stock passera en négatif, l'écart se verra) », chips de toutes les zones (« n'en a pas ») + « hors stock » présélectionné ; zones toutes supprimées mais des livraisons existent → warnbox explicite ; (5) **« le défaut ne devrait pas se propager tant que les fils ne sont pas connectés »** : manchon `a_raccorder` (et bouclé/X, y compris DANS l'antenne aller/retour — `wOpen`) = **coupure** dans `wirePath` (`kind:'cut',open:true`) → le TRACÉ ROUGE s'arrête au premier manchon non serti (`wireTraceSVG` tronque [lo,hi] à la première coupure dans le sens du parcours) et `locate(..., fromD)` refuse une distance au-delà (« ✋ Fil COUPÉ avant cette distance — manchon S-xxx non raccordé ») ; (6) **« au manchonnage les fils sont affichés face à face alors que non »** : la fiche du manchon de SORTIE DE TÉ (élément amont `kind:'teeout'`, ou té avec branche) montre maintenant côté té les **DEUX BRINS du même fil** (couleur du fil du té, sorties latérales « brin AMONT / brin AVAL », libellés des lignes de raccordement adaptés), recolorés au serti vers le fil d'antenne rejoint, **pas d'inversion possible** (badge/err/stepok/save-manchon neutralisés pour ce manchon) ; (7) **« le tracé croise, va pas sur le bon fil »** : dans l'antenne, le repeint prenait la même lettre que le fil parent — il lit maintenant la **conn du manchon de sortie** (brin amont serti sur conn.E) pour choisir le fil aller, l'autre au retour. `TRACE.locate` exposé pour les tests. Tests : e2e_tee passe à **12 checks** (9 rails ≤ jaune ; 10 té retourné pointillé + persisté ; 11 fiche sortie de té brins/pas d'inversion ; 12 coupures open + locate beyondCut), e2e_loop **9 checks** (question stock jamais disparue) ; les 12 suites TOUT VERT ; captures (fiche brins).
- **26/08 (soir) — la fiche manchon dit la VÉRITÉ du té + on sait toujours où est l'amont (3 retours d'Ethan)** : (1) Ethan corrige son propre retour (« sauf du coup l'antenne du té, pardon ») → la fiche « deux brins » de la SORTIE DE TÉ est CONSERVÉE, mais **restreinte au seul manchon de sortie** (`kind==='teeout'`) — les manchons de FÛT contre le té gardent étamé/nu et l'inversion détectable ; (2) **manchon de fût contre le té (son S-2479)** : la coupe montrait les fils « bien en face » alors que la pièce té impose ses sorties (le plan les croisait, pas la fiche → source d'erreur, il fallait « retourner la pièce » à la main) → `teeSideSign(e)` extrait (partagé plan/fiche) + `wireGOf(e,w)` : pour un té à branche, position horaire imposée (plongeur côté antenne → 9 h ou 3 h selon le côté réel de la branche, l'autre fil en face) utilisée par la coupe (`exit`), les libellés des lignes de raccordement et l'ancien formulaire ; les contrôles de ROTATION d'un té dans la fiche manchon remplacés par un rappel « sorties IMPOSÉES par la pièce (…) — té posé à l'envers ? ⤓ retourné dans la fiche du té » ; (3) **mini-vue AMONT/AVAL** (`amontAvalSVG`) au-dessus de CHAQUE schéma de raccordement (étape 2 édition + relecture, ancien formulaire) : le tronçon ±9 m dessiné ORIENTÉ COMME LE PLAN, soudure au centre, flèche du sens, gros « AMONT » / « AVAL » aux deux bouts + rappel « la coupe met toujours l'amont à gauche » — répond à « l'aval à droite à l'envers alors que sur place c'est celui de gauche » ; (4) « le visuel de soudure ne fonctionne pas » : sa capture était l'ANCIEN build (cordon trait fin invisible) — le cordon 3 bandes de l'après-midi règle ça, vérifié en repro (P5 tourné 180°, à reprendre, zoom 180 px/m : bourrelet net entre les deux aciers). e2e_tee **13 checks** (13 : fût contre té = « IMPOSÉES par la pièce » + 9 h/3 h + mini-vue) ; les 12 suites TOUT VERT ; repro + captures (mini-vue AMONT/AVAL, plan cordon).
- **26/08 (soir, suite) — mini-vue v2 (photo aérienne), bouton « retourner le té » dans la fiche manchon, BY-PASS enfin dessiné (acier nu)** : (1) **mini-vue AMONT/AVAL v2** (retour Ethan « fais mieux — une vue photo de l'endroit, un schéma plus précis ») : fond = les MÊMES TUILES IGN ortho que le plan (`tilesFor`+`ignTileURL`, seulement si le chantier est géoréférencé/calé — sinon fond uni), tout le réseau alentour (halo blanc + trait, la ligne courante en évidence), les manchons voisins NUMÉROTÉS avec leur couleur de statut, AMONT/AVAL posés SUR le tracé réel (suivent la courbe), flèche du sens, `preserveAspectRatio slice` h=118 px ; (2) **« je ne trouve pas la fiche du té »** : le hint côté té de la fiche manchon porte maintenant un vrai bouton **« ⤓ té posé à l'envers ? retourner (fils en dessous) »** (`data-teedownj a|b` → l'élément du joint, elLock respecté, persisté pareil) — plus besoin de trouver la fiche du té (qui reste accessible en tapant le CORPS du té sur le plan) ; (3) **by-pass** (sa capture : deux arcs de fils flottants, gaine invisible, étiquette géante) — causes : la gaine était dessinée sur `e.len` (emprise 0,5 m) alors que l'arc réel fait ~2 m (`polyLen`) → **le dessin des éléments utilise la longueur RÉELLE de la polyline (`LaxR`)** (sans effet sur les pièces normales où len≈polyLen) ; l'élément fantôme `U-liaison` (porteur de la soudure, `link:true`) n'affiche plus son étiquette (link transporté par `mk` de setupTraceurLine, label sauté) ; puis Ethan précise « en acier nu le by-pass » → `isSteel` étendu à `kind==='bypass'` : le U se dessine en ACIER APPARENT (comme les SXB), pas de gaine/abouts/fils dessus, la soudure au sommet du U ; captures avant/après. **NOTE 9 h/3 h à caler** : les positions latérales du té dans la fiche sont MON interprétation géométrique (« côté antenne / côté opposé »), PAS une donnée catalogue — Ethan doit dire si le fournisseur documente autre chose (photo/doc d'un té réel côté fût). e2e_tee/dh4/home_safe re-verts après chaque étape ; sweep 12/12 vert.
- **26/08 (midi) — le cas réel S-2505/S-2479 d'Ethan : « 0,73 Ω / étamé 608 m c'est impossible » (3 bugs + 1 clarification physique + mini-vue v3)** : il a bouclé les fils ENTRE EUX au manchon de SORTIE DE TÉ S-2505, puis mesuré depuis S-2479 (parente) : l'appli affichait « VERS L'AVAL 0,73 Ω — fermée à S-2505 — étamé 608,2 m + nu 0,5 m ». (1) **BUG null** : S-2505 (soudure d'antenne) n'est pas sur le parcours étamé → `r.dE=null` → `Math.abs(608,2 − null) = 608,2` (JS !) → `mk()` de dhAtPoint teste maintenant les 4 distances (dE ET dN, row ET r) — plus JAMAIS une soustraction avec null ; (2) **PHYSIQUE** : boucler les fils entre eux à une sortie de té ne ferme PAS la boucle de la parente — côté amont de ce manchon, les deux « fils » sont les 2 BRINS du fil parent : les ponter = ISOLER l'antenne et assurer la CONTINUITÉ du fil parent à travers le té → `antennaMode` : `tj.loopA||tj.loopB` ⇒ mode 'boucle' (l'antenne sort du tableau de la parente, le fil parent traverse sans coupure — au détour du té près, ~1 m négligé, noté ; l'antenne devient une boucle À PART fermée à sa tête, listée dans le sélecteur DH) → depuis S-2479 vers l'aval : « ouverte — fils non raccordés à S-xxxx » = la vérité (pour lire une boucle depuis S-2479 il faut un pont ⟲ étamé↔nu en aval ou un bout bouclé) ; (3) **« Localiser un défaut » disait « Distance supérieure à la longueur de fil connue (5 583,9 m) » pour 61 m** : c'était mon `beyondCut` non affiché par ce bloc → vrai message « ✋ Fil COUPÉ avant cette distance — manchon S-xxx non raccordé / bouclé » ; (4) **positions té tranchées par Ethan** : « 10 h / 2 h pour les tés aussi, d'où l'importance de pouvoir les retourner » → `wireGOf` = clockPos, té retourné (⤓) = miroir haut/bas (8 h/4 h) ; hint « té — pas de rotation libre, fils à 10 h/2 h (ou 8 h/4 h retourné) » + bouton ⤓ conservé ; le 9 h/3 h latéral abandonné en coupe (le PLAN garde le plongeur côté antenne — validé par lui) ; (5) **mini-vue v3** (« on voit pas grand-chose, cliquer pour agrandir ») : fenêtre ±19 m, voile plus léger, labels affinés, badge « 🔍 agrandir », **tap → modal plein écran ±46 m** (mêmes tuiles/réseau/manchons) + bouton « Ouvrir le PLAN à cet endroit » (closeSheet + centerOn). e2e_tee **14 checks** (13 refait : 10 h/2 h + bouton ⤓ + mini-vue ; **14 = le scénario S-2505** : antenne isolée du tableau parent, pas de fausse fermeture, pas de coupure du fil parent) ; sweep 12/12 TOUT VERT ; capture de preuve (VERS L'AVAL « ouverte — fils non raccordés », Localiser 61 m → « fil coupé au manchon S-0007 »).
- **26/08 (aprem) — cohérence plan ↔ coupe au té, « cuivré » partout, AMONT/AVAL toujours visibles (4 retours d'Ethan)** : (1) **le PLAN contredisait la coupe** (il re-signalait « les fils en face alors qu'ils ne le sont pas — mais pas de l'autre pour les fils du té ») : le plan forçait encore les fils du té « latéral côté antenne » pendant que la coupe affichait 10 h/2 h → le plan passe aux positions RÉELLES `wireGOf` (10 h/2 h, ⤓ retourné = 8 h/4 h **en pointillé dessous**) — seuls les RAILS plongent vers l'antenne (clamp conservé) ; la coupe montre le retourné EN BAS de la tranche (8 h/4 h sous l'équateur — « la perspective montre qu'il est en dessous ») ; e2e_tee c7 allégé (départs des rails plus forcés côté antenne) ; (2) **« nu » → « CUIVRÉ » dans TOUTE l'interface** (WIRE.short/label, légende du plan, selects de raccordement, onglet DH : tableau/en-têtes/messages/« Localiser », fiches, teeUI — la clé technique `N` et les données ne bougent pas ; le traceur maquette/ garde ses libellés, à passer si demandé) ; leçon regex : les chaînes JS `l\\'étamé` s'ancrent AVEC le backslash ; (3) **mini-vue : AMONT et AVAL toujours visibles tous les deux** (sa capture : AVAL hors cadre sur tronçon vertical) : le cadre épouse le ratio du viewBox (`aspect-ratio` + `preserveAspectRatio meet` → plus AUCUN recadrage, petite vue max-h 175 px / modal 58 vh) et les deux labels sont CLAMPÉS dans l'image (`clampL`) ; RY petite vue 9,5, modal 34 ; (4) boucles : « ça a l'air ok » ✓. Sweep 12/12 TOUT VERT ; captures (plan té 10 h/2 h + retourné pointillé, coupe retournée 8 h/4 h en bas, mini-vue clampée).
- **26/08 (fin d'aprem) — tés à angle AIGU assainis + AMONT/AVAL réellement réglé (2 retours d'Ethan)** : (1) **« toujours des merdes sur mes tés »** (reproduit : antenne qui sort presque PARALLÈLE à la parente) — deux causes : le T PLEIN à congés et l'ABOUT de branche partent en vrille quand la branche est couchée sur le fût, et le raccord de plongée faisait une grosse diagonale en travers → `perpT` (angle branche/fût < ~55° = trop couché) : T plein et about SEULEMENT si la branche est franche, sinon branche dessinée en tube noir droit simple ; about aussi conditionné à `vL > bague+acier+0,25` ; et le **raccord de plongée** (fil → bord de branche, il traverse la MOUSSE dans le corps) est maintenant DISCRET (`data-wteec`, pointillé fin opacité .5) — les rails le long de la branche restent pleins ; e2e_tee c1/c7 adaptés (rails = M-L + raccords data-wteec ≥ 2 ; c7 teste le départ B du rail clampé dans le corps) ; (2) **« amont aval c'est pas réglé » — il avait raison** : le correctif AMV4 (aspect-ratio + meet + clamp des labels) était chaîné DERRIÈRE le patch cuivré qui a crashé → jamais appliqué ni rejoué (leçon : après un crash d'assert, REJOUER TOUTES les commandes du bloc, pas seulement celle qui a crashé) → appliqué pour de vrai : le cadre épouse le ratio du viewBox (`aspect-ratio`, `meet`, petite vue max-h 185 px / modal 58 vh), labels clampés (`clampL`, x1/y1 ajoutés), vérifié en repro pire cas (tronçon plein nord : AMONT en haut, AVAL en bas, tous deux dans le cadre, petite vue ET modal). Sweep 12/12 TOUT VERT.
- **Pose en drapeau (cadrage initial)** : sur certains tronçons les deux conduites sont l'une au-dessus de l'autre (même position en plan). À prévoir : attribut « drapeau » sur une plage de PK d'une ligne (début/fin posés via ＋), conduite du dessus à choisir, entraxe vertical, et à chaque transition un **jeu de coudes en altimétrie** par conduite qui bouge (2 coudes en élévation, + le rabattement latéral vers l'axe : coudes 3D ou 2 + 2). Le traceur compte les pièces et les soudures des transitions et dessine en plan les deux conduites superposées (dessous en trait discontinu) ; une petite vue en élévation (z / chaînage) au droit des transitions. Proposition envoyée à Ethan avec questions : motif de transition (2 × 45° ? 2 × 90° ? coude de remontée), qui passe dessus/dessous, entraxe vertical, longueur de transition.
- Reste (v2, à cadrer avec Ethan) : retouche pièce par pièce (remplacer un coude par 1×2 m, recouper une barre, coulisser une soudure), export vers l'appli TRACÉ (format chantier), lyres proposées, DN/série par tronçon.

## Plans reçus le 17/08/2026 (dans `plans/` du dépôt d'Ethan, hors Git ; copies dans le conteneur `~/plans/`)
| Fichier | Contenu réel | Profil | Notes |
|---|---|---|---|
| RCU Bain de bretagne PRO V4.dwg (34 Mo, R2018) | **Plan projet du BE (Mensura), pas le calepinage LOGSTOR** : un axe aller en 339 POLYLINE (calque `Ass - Projet RCU Aller - Réseau`), 351 nœuds `Default_SymbolReg` (calque `… - Regards`, les tronçons s'arrêtent au bord du symbole : 6–30 cm), étiquettes DN par tronçon en blocs `MENSURA_ALIGNEDTXT_*` (texte lettre par lettre « D,N,1,0,0 »), 25 vannes (`Vanne RCU simple` / `double purge`, calque `PROJET RCU - Equipements`), 5 blocs `Réduction`, doublon du tracé sur `PROJET RCU - Tracé Projet RCU`, légende à 5 km au nord (calques `RCU - PRO - *`, `RCU - COUPE - *`), 2 xrefs liés `FUTUR école` (180 k entités chacun, tronqués). Lambert CC48. | mensura-axis | Import brique 1 : 21 lignes (2 racines, 19 antennes), 4 522 m, DN 150→125→100→80→40 sur la principale, 972 éléments, 951 soudures. |
| Renalia_Saint-Lo_Tranche Jaune_V4_22.07.2026(1).dwg (16 Mo, R2010) | Renalia : axes `CANALISATION` (420 LWPOLYLINE), calque `Renalia` (1 264 blocs dont `10090+`, `4090+`, `3290+`, anonymes `*U…`), étiquettes « ALLER DN65 ENV. DN160 » | renalia | = le fichier déjà extrait hors appli en `sites.json` (saintlo). |
| Renalia_Saint Lô_Zone Bleue_V18_30.07.2026.dwg (43 Mo, R2010) | Renalia, structure différente : un calque par tronçon avec le DN dans le nom (`A_B_DN300`, `Q_Q1_DN65`, `SST004_DN40`…) contenant les blocs de pièces (`kj160`, `kj125`, `tm125`, `5090+`, `3290+`, `dhec32+`), axes `PRO_CH_A_Axe` / `PRO_CH_R_Axe`, 1 xref tronqué | renalia | Jamais reçu avant. Le plus prometteur pour une lecture déterministe Renalia. |
| CHA00071-INDRE_Ind A.dwg (10 Mo, R2018) | JBTP/Mensura, blocs `Présentation` (calques internes Tube/Isolation/Axe), coudes 15/20/30/60/90°, tés, manchons, réductions « DN80 x 50 - ISO ST », étiquettes « DN50 (60,3/125) », AXIOM | jbtp | **C'est le « Nantes » des notes précédentes** (mêmes 54 pièces à 243 km ignorées). Import : 268 pièces, 16 lignes, 249 éléments, 233 soudures. |

DXF allégés pour tests (`tools/dxf_filter.py`, calques du réseau + blocs référencés, 5–20 Mo) déposés dans `plans/` d'Ethan : `bain_reseau.dxf`, `saintlo_jaune_reseau.dxf`, `saintlo_bleue_reseau.dxf`, `indre_reseau.dxf`.

## Conversion DWG → DXF (important)
LibreDWG **0.13.3 publiée échoue** sur Bain PRO, Saint-Lô jaune et bleue (« Invalid sections », CRC mismatch, R2010/R2018) et écrit les noms de blocs anonymes tronqués (`*U` au lieu de `*U91` → les INSERT JBTP ne retrouvent plus leur bloc). La **version git (0.14.x, août 2026)** convertit les 4 plans (7 à 13 s, DXF de 260 à 470 Mo). Compilée dans le conteneur (`/usr/local/bin/dwg2dxf`), recette dans `test/README.md`. Sur le PC d'Ethan : ODA File Converter en attendant le WASM dans l'appli.

## Construire / publier
`cd app && npm install && npm run build` → `dist/index.html` → copier à la racine du dépôt (`index.html`) → GitHub Pages sert la nouvelle version en une minute.
Tests : `app/test/` (Node + Playwright, voir `test/README.md`) : `parse_test`, `an_test`, `build_test` (module DXF seul) et `e2e_import` / `e2e_view` (import complet dans `dist/index.html`, captures, notes du moteur). L'appli expose `window.TRACE` pour le débogage. Rôle chef/bureau requis pour voir le bouton Importer (`#roleSel` = ethan).

## Poste de travail d'Ethan (mis en place le 17/08/2026)
- Dépôt cloné sur son PC (`scr-70`, Windows) dans `C:\Users\EthanLEBIHAN\Dev\RCU-COMPAGNON` — **hors OneDrive** (son dossier `SCR` et ses Documents sont synchronisés SharePoint, incompatibles avec Git).
- Il publie avec **GitHub Desktop** : saisir un résumé → `Commit to main` → `Push origin`. Il ne construit pas lui-même ; l'assistant construit et dépose `index.html` dans le dossier connecté.
- Ethan part de zéro en code : détailler chaque manipulation (nom exact des boutons, chemins complets), et ne jamais lui faire taper un chemin quand un bouton « Choose… » existe.

## Historique des correctifs
- **17/08/2026 (soir, suite) — « coude contre coude » (retour d'Ethan : à chaque coude, un paquet de bouts de 0,1 m).** Cause : (1) `genLine` donnait aux coudes une emprise de 0,5 m sur l'axe alors que le moteur les pose avec leurs jambes catalogue (1 m + 1 m) ancrées au début de cette emprise → chaque coude était décalé de 0,75 m et la barre libre voisine se faisait étirer/recouper n'importe comment ; (2) rien ne reconnaissait deux coudes dessinés sommet à sommet. Fait : emprises catalogue dans `genLine` (jambes de coude, L de té, réduction 1 m, vanne 1,5 m ; emprises qui se chevauchent → frontière au milieu, pas de barre), `corner` (sommet) porté jusqu'au moteur, règle 3b dans `recognize()` : deux coudes dont les sommets sont à moins de « jambe + jambe + 0,5 m » → le second **suit** le premier (`follow` : posé sur le port de sortie, plus sur son ancre du plan) ; note `interp` « coude contre coude » si l'écart ≤ 0,35 m, sinon `doubt` « chicane courte, jambes à recouper de x m ? » ou « chicane trop courte pour les pièces catalogue » (nouveau genre de note ❓ dans le rapport). `bars()` : jamais plus de 12 m, chute ≥ 1 m (l'avant-dernière barre est recoupée sinon) ; après le premier `solve`, une barre libre étirée > 12 m est coupée en 12 + chute. Bain PRO : barres > 12 m 3 → 0, séquences coude-bout-coude 154 → 36 (les restantes sont de vraies lyres à 3 m de sommet à sommet), fragments < 0,5 m 108 → 41. Le chantier pré-chargé Bain (LOGSTOR) reconnaît 196 coude-contre-coude, 11 doutes.
- **17/08/2026 (soir) — retour d'Ethan après essai sur la zone bleue : « catastrophe, les calques c'est nul ».** Ce qu'il veut, mot pour mot : *voir les deux tubes du plan et redessiner notre réseau par-dessus avec les pièces du catalogue ; les calques/étiquettes on s'en fout ; dessiner à la règle, à peu près rectiligne sauf quand il y a vraiment des angles sur le DWG ; à l'échelle, fidèle au plan d'origine ; assemblé uniquement de pièces du catalogue.* Corrections immédiates : (a) **fond de plan sur tous les imports** (`buildDrawing` : calques du réseau en foncé, contexte voirie/bâti/cadastre en clair, jamais la topo ni les symboles répétés > 250 fois, plafond 45 000 points, fenêtre = emprise du réseau + 40 m) — il manquait sur les imports par axes, d'où « je ne vois pas les tubes » ; (b) **choix automatique des calques** (un aller + un retour, jamais un « axe unique » quand les deux existent, jamais un calque sans tracé, dédoublonnage géométrique sauf entre aller et retour qui se longent), la liste des calques est repliée dans « changer » ; (c) **règle** : lissage Douglas-Peucker 0,35 m des lignes lues (les petites cassures du dessin disparaissent, les vrais angles restent) ; (d) zone principale par amas de cellules connexes de 500 m (au lieu d'une fenêtre fixe qui coupait la zone bleue) ; (e) pontage des interruptions d'axe < 4 m alignées (symbole/pièce dessinée sur l'axe) ; (f) DN lu dans le nom du calque des blocs (`A_B_DN300`). Ce qui reste (brique 3) : les chicanes/coudes bizarres aux nœuds → doutes ; Renalia toujours fragmenté (187–230 lignes) tant que le profil Renalia n'est pas écrit.
- **17/08/2026 — brique 1 « l'appli ouvre les vrais plans » (dxfimport.js réécrit, app.js import).** `createDXFParser` (automate code/valeur, POLYLINE+VERTEX enfin lus — l'ancien parseur les jetait —, INSERT+ATTRIB, MTEXT multi-lignes, blocs plafonnés à 20 000 entités = xrefs tronqués et signalés), `parseDXFFile` (flux, détection UTF-8 / windows-1252, progression), `analyze()` (profil jbtp / logstor / renalia / mensura-axis / generic, `axisCandidates` cochables, étiquettes DN dans les blocs et attributs), `buildSite` v2 (zone principale, nœuds, graphe, DN par tronçon `dnSegs`, spéciaux vanne/réduction/té, réduction implicite au changement de DN), `previewSVG` pour les axes, `genLine` (app.js) barres coupées aux changements de DN + réductions/vannes typées, `othersOfLine` corrigé (plantait sur tout chantier importé par axes → aucun moteur ne se lançait), `dn2` des réductions porté jusqu'au moteur.
- **17/08/2026 — `ensureSite()` réintégré.** Le dépôt contenait un `index_2.html` (build plus récent uploadé sous un autre nom parce que GitHub refusait d'écraser `index.html`) porteur d'un correctif absent des sources et absent de la version en ligne : `sync.ensureSite()` crée la fiche chantier dans la table `sites` (upsert `onConflict:'id'`, `ignoreDuplicates:true`, `data:{builtin:true}`) **avant** tout `saveWeld` / `saveLineState`, sinon les soudures des chantiers pré-chargés (Bain, Saint-Lô — jamais passés par l'import) ne remontent pas au serveur. `loadSites()` filtre désormais ces fiches `builtin` (sans `lines`) pour qu'elles n'apparaissent pas comme des chantiers vides. Correctif reporté dans `src/sync.js` + `src/app.js` ; build reconstruit identique au bit près à `index_2.html` (md5 `e57dde300c8924b601df5cb44639dc9b`), ce qui valide la reconstitution. `index_2.html` supprimé.
- **17/08/2026 — rangement du dépôt.** Les sources sont passées de `RCU-COMPAGNON_source/app/` à `app/` à la racine ; le doublon `RCU-COMPAGNON_source/index.html` supprimé. Ajout d'un `.gitignore` (`node_modules/`, `app/dist/`). Règle : **un seul `index.html`, à la racine** — c'est le seul fichier servi par GitHub Pages.
